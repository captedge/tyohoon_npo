# completed-log.md — 完了記録

宣言2（記録の4分割）の④を担当。1件につき日付・結論1〜2行。詳細は該当`docs/devlog-<テーマ>.md`にリンクする。

書式：
`**タイトル**（日付、コミットハッシュ、該当すればブランチ名）— 結論1〜2行。詳細は`docs/devlog-xxx.md`（devlogを作らない軽微な変更なら省略）。`

**モバイル版カーソル緯度経度表示（長押しクロスヘア）を実装、実機確認で3回のフォローアップを経て確定**（2026-08-02）— デスクトップ版のマウスカーソル緯度経度表示（`_cursorLatLon`）に相当する機能をモバイルに追加。長押しで赤十字カーソルが出現しbottom-right readoutに緯度経度を表示、短タップで解除。フォローアップ①「モード中も普通のドラッグで動かしたい」→`onPanStart`/`onPanUpdate`を追加。フォローアップ②「ズームは有効のまま試したい」→`InteractiveViewer.scaleEnabled`を常時trueに変更し、ズーム中も`_syncFromController`でcrosshairの緯度経度を再計算。フォローアップ③「2本指ではズームが一切効かずカーソルが反応する」→原因は`onPanStart`/`onPanUpdate`（`GestureDetector`の`PanGestureRecognizer`）が1本目の指でジェスチャーアリーナを独占し、`InteractiveViewer`側のピンチ認識が2本目の指を一度も基準点と組み合わせられなくなっていたこと（スワイプ廃止時と同種の問題）。`Listener`（生ポインタイベント観測のみ、アリーナに参加しない）＋ポインタ数トラッキング（`_mobileCursorActivePointers`）に置き換えて解消。フォローアップ④「触れ直すとカーソルがジャンプする、指の下に隠れて見づらい」→トラックパッドと同じ相対移動方式に変更（`_mobileCursorLastPointerPos`で指の移動量＝デルタを計算し、その分だけカーソル位置に加算。触れた瞬間はカーソルを動かさない）。ユーザー実機確認「OK,Goodです」。括弧バランス検証は各段階で実施。詳細は`docs/devlog-mobile-flutter.md`「モバイル版カーソル緯度経度表示」参照。

**モバイル専用UI一式：凡例位置を最終的にデスクトップと同じ位置に確定、実機確認完了**（2026-08-02）— 「Passage Plan＝右下・Typhoon＝左下へ移動」の実装はユーザー自身の伝え間違いと判明し全面撤回、最終的にPassage Plan＝左上（`left:48,top:40`）・Typhoon＝右上（`right:64,top:40`、波高バロメーターの`right:64`と右端が揃う）というデスクトップと同じ位置に確定。あわせて、モバイル向け50%縮小の実装当初は位置オフセット自体も`scale`倍していたことが、最初の「グリッドラベルと重なる」報告の真因だったと判明（グリッドラベル側は縮小されないため、オフセットだけ半分にすると実クリアランスも半分になっていた）——位置オフセットは常に非スケール、カード内部のコンテンツ（フォント・パディング）のみスケールする方式に修正。ダブルタップでのメニュー表示（当初のスワイプ方式は実機で地図パンと競合したため廃止・置き換え済み、「ダブルタップ：良好です」）、Passage Planダイアログのスクロール対応（固定460×420で画面に収まらない問題を解消）とあわせ、ユーザーが実機で「OKでした！」「モバイルもほぼ完了です」と総括確認。全画面地図表示・波高バロメーター50%縮小・右側ズームバー削除（ピンチズームで代替、カーソル機能の実機テストでピンチ動作自体は確認済み）についても個別の異議なし。`main.dart`の横向き固定（`SystemChrome.setPreferredOrientations`）も、これら一連のモバイルUI実機テストが横向きレイアウトで問題なく行えたことから機能していると判断。詳細な経緯は`docs/devlog-mobile-flutter.md`「凡例位置の移動・Passage Planダイアログのスクロール対応」「位置の最終訂正」参照。

**モバイル対応（Android先行）着手、UI方針の再確認と本流／個人ビルド分岐の追加確定**（2026-08-02）— TASKS.mdに残っていた「着手時に改めて確認」に従いAskUserQuestionで再確認し、デスクトップ版と機能・デザインを共通化する仮方針を継続確定。加えてWindows版と同様の本流／個人ビルド分岐をAndroidにも用意する方針を新たに確定。ブランチ運用（`feature/mobile`）はユーザーが判断に迷ったためClaude側で既存方針を採用。`main.dart`に横向き固定（Android/iOSのみ）を実装、`pubspec.yaml`にビルド番号追加（`0.1.0+1`）。詳細・技術面の事前確認事項・今後のステップは`docs/devlog-mobile-flutter.md`参照。

**モバイル専用UI一式を改訂：スワイプ→ダブルタップに変更、凡例位置を下部に移動、Passage Planダイアログをスクロール対応に**（2026-08-02）— `build_apk_personal.bat`での実機/エミュレータ確認を受けたユーザーからのフィードバック3点を反映。①スワイプ判定が地図の移動操作（パン）と競合したため、当たり判定の大きさを変えても解決しないとの実機報告を受け、上下端スワイプ方式を完全に廃止し、地図上のダブルタップで表示する方式に変更（`_isMobileUi`時のみ`onDoubleTap`を地図の`GestureDetector`に追加、`null`のときは`DoubleTapGestureRecognizer`自体が生成されないためデスクトップの既存タップ挙動には一切影響しないことをAgentレビューで確認）。②「Passage Plan」「Typhoon」凡例枠が緯度経度グリッドラベルと重なる問題を修正——モバイルのみ、Passage Plan凡例を右下（`right:12, bottom:160`）、Typhoon凡例を左下（`left:12, bottom:12`）に移動（`bottom:160`は右下の別クラスタ〈波高バロメーター・日時表示〉と重ならないための目測値、実機確認要）。③Passage Planダイアログの内容一覧がウィンドウ固定（460×420）で画面に収まらない問題を修正——`SingleChildScrollView`＋`ConstrainedBox`（画面高さの80%を上限）でダイアログ全体をスクロール可能にし、内部の`ListView.separated`はインラインのcollection-forループに置き換え（二重スクロール構造を避けるため）。実装後にAgentツール（general-purpose）による独立レビューを実施——must-fix指摘なし、軽微な指摘（コメント中に残っていた旧「スワイプ」表記3箇所）はその場で修正。文字列・コメントを除外した専用スクリプトで丸括弧・波括弧・角括弧の対応が一致することを確認済み。Coworkサンドボックス（Dart SDKなし）のためコンパイル確認は未実施——Windows機での実機/エミュレータ確認が必要（TASKS.md参照）。詳細は`docs/devlog-mobile-flutter.md`参照。

**モバイル専用UI一式を実装（全画面地図・スワイプ式チュートリアルバー・ズームバー削除・凡例類50%縮小）**（2026-08-02）— ユーザー要望5点（①全画面地図表示、②メニューバー/再生バーを上下端スワイプで出現・2秒自動非表示・地図オーバーラップ、③波高バロメーター50%、④右側ズームバー削除、⑤台風名称枠/航路名称枠50%）を実装。`_isMobileUi`（Android/iOSのみtrue）で分岐し、デスクトップ版の見た目・挙動は完全に維持。実装の要点：`Scaffold.appBar`をモバイルでは`null`にし同じ`AppBar`を`_buildAppBar()`として抽出・共有、`body`を外側`Stack`に再編して既存のColumn（地図＋再生バー）とモバイル専用オーバーレイ層（`_buildMobileChromeOverlays`）を重ね、地図のColumn/Expandedサイズ自体はチロム表示状態に関わらず変化しない設計。上下端のスワイプ検知はGestureDetectorではなく`Listener`（生ポインタイベント）を採用——InteractiveViewer側のパン/ピンチのジェスチャーアリーナと競合させないための意図的な選択。実装後にAgentツール（general-purpose）による独立レビューを実施——must-fix指摘なし、軽微な指摘3件のうち1件（再生バーが無い状態で下端スワイプがAppBarを無意味に再表示していた）はその場で修正、残り2件（複数指同時操作時の座標混線、台風凡例ボックスの高さ上限計算がモバイルで若干保守的すぎる）は実害が小さいため保留、`docs/devlog-mobile-flutter.md`に記録。文字列・コメントを除外した専用スクリプトで丸括弧・波括弧・角括弧の対応が一致することを確認済み。Coworkサンドボックス（Dart SDKなし）のためコンパイル確認は未実施——Windows機での実機/エミュレータ確認が必要、特にスワイプの当たり判定サイズ（`_mobileEdgeZoneHeight`＝72px、`_mobileSwipeThreshold`＝18px）はOS側ジェスチャーとの競合有無を見ながらの実機調整が前提（TASKS.md参照）。

**`file_picker`のcompileSdk要求によるビルド失敗を実際に踏んで特定・修正**（2026-08-02）— ユーザーが`build_apk_personal.bat`を実行したところ`:file_picker:checkReleaseAarMetadata`が失敗（`file_picker`の依存`flutter_plugin_android_lifecycle`がcompileSdk 36以降を要求）。事前に対策済みだった`file_picker`のKotlinプラグイン未適用問題（別問題）とは異なる、新たに踏んだ既知パターン（ShipsTimeが`alarm`パッケージで経験した同種のcompileSdk要求問題）。`android/app/build.gradle.kts`の`compileSdk`を`maxOf(flutter.compileSdkVersion, 36)`に変更し、`android/build.gradle.kts`に`:app`以外の全サブプロジェクトのcompileSdkを36に強制する`subprojects`ブロックを追加（ShipsTimeの実装をそのまま移植）。丸括弧・波括弧の対応を機械的に確認済み。詳細・教訓は`docs/flutter-android-env-notes.md`「1b」参照。ユーザーに`build_apk_personal.bat`の再実行を依頼中、成功可否は未確認。

**`flutter create --platforms=android .`実行完了、android/生成後の必須対応（INTERNET権限・file_pickerワークアラウンド・NDK除外）を実装**（2026-08-02）— ユーザーがWindows機で`create_mobile_branch.bat`実行（`feature/mobile`ブランチ作成）後、`flutter create --platforms=android .`を実行し`android/`一式が生成された（Gradle 9.1.0・JVM17、`gradle.properties`のJVMメモリ設定は既にこのマシンのテンプレート既定値として同梱済みと判明——ShipsTimeで手動追加したものではなかった）。生成直後に3点を実装：①`AndroidManifest.xml`に`INTERNET`権限追加（JMA/JTWC/Open-Meteo通信用）、②`android/build.gradle.kts`に`file_picker`既知バグの回避コードを予防的に追加（`docs/flutter-android-env-notes.md`参照）、③`android/app/build.gradle.kts`の`ndkVersion = flutter.ndkVersion`行を削除（本プロジェクトの依存パッケージはNDK不要、約750MBの不要ダウンロードを回避）。丸括弧・波括弧の対応を機械的に確認済み（Coworkサンドボックスにdart/gradleが無いためコンパイル確認は未実施——Windows機での`run_android.bat`等の実行確認が必要）。`android.overridePathCheck`はこのマシンのテンプレートにデフォルトで含まれていないことを確認、根拠不明なため引き続き予防的追加は見送り。

**別プロジェクト「ShipsTime」からAndroidビルドの既知の落とし穴・スクリプトを調査・移植**（2026-08-02）— 同じWindows機で先行してAndroidビルドを経験済みの別プロジェクトを調査し、本プロジェクトに関係する事項を`docs/flutter-android-env-notes.md`に集約。特に、本プロジェクトも使用中の`file_picker`プラグインでShipsTimeが実際に踏んだ既知の不具合（Kotlin Androidプラグイン未適用によるビルド失敗）とその対策コードを発見し、`android/`生成後に予防的適用する計画をTASKS.mdに追加。あわせてShipsTimeの動作実績があるスクリプトを本プロジェクト向けに調整して新設：`create_mobile_branch.bat`（ブランチ作成・push）、`run_android.bat`／`run_android_personal.bat`（エミュレータ起動・実行、共有AVD`ShipsTime_Test`を使用）、`build_apk.bat`／`build_apk_personal.bat`（リリースAPKビルド）、`fix_emulator_window.ps1`（エミュレータウィンドウ位置補正）。全`.bat`ファイルはCRLF・ASCII文字のみ・丸括弧の対応を機械的に確認済み。`android/`フォルダ生成前のため実行確認は未実施。詳細は`docs/devlog-mobile-flutter.md`参照。

**保存先をWindowsのper-user AppDataフォルダから、exe相対のDataフォルダ（ポータブルzip内）に変更**（2026-08-01）— ユーザーから「zipフォルダを別PCにコピーしてもデータが引き継がれない」との指摘を受け、原因（`shared_preferences`/`path_provider`がOSのユーザープロファイル配下を指すため）を確認した上で、トレードオフ（`flutter clean`実行時に消える、Debug/Releaseでデータが別になる）を説明・合意の上で実施。新設`lib/utils/portable_storage_dir.dart`＋`app_state_storage.dart`／`csv_library.dart`／`wave_field_cache.dart`の保存先切替、旧保存先からの一度きりの非破壊移行、`build_release.bat`／`build_release_personal.bat`のzip化時の`Data`フォルダ除外（開発者のローカルテストデータ混入防止）を実装。Agentレビューで指摘2件（PowerShellの`-Exclude`がワイルドカード無しで効いていなかった／`app_state_storage.dart`の`load()`のtry/catchスコープ漏れ）を発見・反映済み。**追記（2026-08-01、ユーザーがWindows機で実際にビルド・実行し発覚）**：フォルダ名を当初`Data`にしていたところ、Flutterがビルド時にexeの隣へ自動生成する必須フォルダ`data`（`icudtl.dat`/`flutter_assets`一式）とWindowsの大文字小文字非区別により衝突し、zip除外スクリプトがこの必須フォルダごと除外してしまってアプリが起動しなくなる不具合が発生（ユーザー報告「アプリが立ち上がらないです windows11」）。フォルダ名を`UserData`に改名して解消。原因・教訓は`docs/devlog-portable-data-dir.md`に追記済み。**追記2（2026-08-01）**：ユーザーの実機検証で、`UserData`への改名後も展開したexeが起動しない状態が継続していることが判明。切り分けの結果、`build\windows\...\Release\`のexe直接実行は成功する一方、生成zipには`data`フォルダ（Flutter本体）が含まれていないことが発覚——`Get-ChildItem | Compress-Archive`が大量ファイルを含む`data`フォルダをエラー表示なしに欠落させていたのが原因。zip化を`robocopy`で短いパスへステージングしてから`Compress-Archive`する方式に変更し、ユーザーがWindows機で再ビルド・展開・起動を確認し**起動成功**。原因・再発防止ルール（保存先命名チェック・zip化の注意）は`docs/devlog-portable-data-dir.md`・`docs/operation-rules.md`に追記済み。残りの確認項目（データ移行・別PCコピー・zip内容の完全性）は引き続きTASKS.md参照。

**台風との距離表示（"N nm"ボックス）の文字色を白から黒に変更**（2026-07-31、`main`ブランチ、commit `6b623a6`）— `map_painter.dart`の`_drawDistanceLabel`内`TextStyle`1箇所のみの変更。ボックス自体の塗り（台風の色、不透明度0.92）・黒枠は変更なし。単一プロパティの変更のためAgentレビューは省略、丸括弧・波括弧・角括弧の対応チェックのみ実施（一致）。Coworkサンドボックス（Dart SDKなし）のためコンパイル確認は未実施——Windows機での実機確認が必要（黒文字が台風の塗り色の上で視認しやすいか確認）。

**船アイコンをユーザー提供の色別10種専用画像（`ship_01.png`〜`ship_10.png`）に切替、旧`ColorFilter`着色方式を廃止**（2026-07-31、`main`ブランチ、commit `6b623a6`）— `kShipColorPalette`と同順であることを各画像の平均RGB照合で確認した上で実装。表示サイズ（旧比±0.4%以内）・座標アンカー（下から15%→25%、新画像の余白増加分を補正し船体上の相対位置は旧同等）はいずれも数値計算で根拠付け。実装中にリスト要素の直接書き換えでは`shouldRepaint`が変化を検知できない潜在バグを自己発見・修正。Agentレビュー実施、重大な指摘なし。詳細・計算根拠は`docs/devlog-2026-07-31-ui-polish-and-wave-field-design.md`「⑥」参照。確認済み：文字列内挿入を正しく処理する専用スクリプトで丸括弧・波括弧・角括弧の対応が一致。Coworkサンドボックス（Dart SDKなし）のためコンパイル確認は未実施——Windows機での実機確認が必要（TASKS.md参照）。

**Passage Plan編集画面の緯度経度入力を10進度から度分（deg-min）形式に変更**（2026-07-31）— 地図画面のカーソル表示と統一した「35-15.50」形式に変更、新規`lib/utils/deg_min_format.dart`に変換ロジックを集約。詳細は`docs/devlog-2026-07-31-ui-polish-and-wave-field-design.md`参照。

**波の場オーバーレイの波高色スケールを6段階（青→緑→黄→橙→紫→赤）に再設計**（2026-07-31）— 中間レンジの見分けにくさを解消。詳細は`docs/devlog-2026-07-31-ui-polish-and-wave-field-design.md`参照。

**波の場オーバーレイの波高色スケールを0-4mから0-8mに拡張**（2026-07-31）— 台風時の高波表示に対応。詳細は`docs/devlog-2026-07-31-ui-polish-and-wave-field-design.md`参照。

**波の場オーバーレイに波高カラーバー凡例（バロメーター）を追加**（2026-07-31）— 右下に実データと対応する色スケールを自動表示。詳細は`docs/devlog-2026-07-31-ui-polish-and-wave-field-design.md`参照。

**再生バー終点の定義を変更：「台風の最終予報時または航海終了時のうち遅いほう」に**（2026-07-31）— `_maxOffsetHours`を条件分岐なしの`math.max`に簡素化。詳細は`docs/devlog-2026-07-31-ui-polish-and-wave-field-design.md`参照。

**波の場オーバーレイ：波高等高線は撤回、流れ方向はシェブロン（">"が流れて動く）方式に変更**（2026-07-31）— 等高線・矢じりストリークは実機確認後にユーザー判断で撤回・完全削除。詳細は`docs/devlog-2026-07-31-ui-polish-and-wave-field-design.md`参照。

**台風の軌跡情報ボックスがズームバーと干渉する不具合を修正**（2026-07-31）— 当初は再生バーとの干渉と誤診断、ユーザー訂正を受けて真因（ズーム列との重なり）を特定し`right: 64`に修正。詳細は`docs/devlog-2026-07-31-ui-polish-and-wave-field-design.md`参照。

**仕様変更書（specification_change_0731_01.docx）の実装状態を検証、孤立アセットの削除とCLAUDE.md/TASKS.mdの実態同期**（2026-07-31）— 7〜8項目のうちVPCY51孤立アセット削除以外は既に実装済みと確認。詳細は`docs/devlog-2026-07-31-ui-polish-and-wave-field-design.md`参照。

**波の場オーバーレイ（固定エリア・手動Import方式、同時実行数対策、密度500点への修正）をWindows実機で確認完了**（2026-07-31）— ユーザーが`run_windows_personal.bat`で実機確認し「Goodでした！！やっと出来上がりました」と報告。Display/Import UI、固定エリア（25N-45N/125E-150E）のグリッド取得・表示、「Too many concurrent requests」「Minutely」いずれのエラーも発生せず最後まで取得できること、パン・ズームで再取得が起きないこと、タイムバー連動の色変化、Save/Cancel挙動、再起動後の状態復元、密度500点（間隔約1.0度）の見た目、いずれも問題なしとの確認を得た。これで2026-07-30〜31の一連の再設計（下記4項目：固定エリア化→自動継続廃止→同時実行数制限→密度修正）が完結。詳細経緯は`docs/devlog-online-xml.md`「Open-Meteo個人用試用機能・波の場オーバーレイの実装と今後の方針」参照。

**波の場オーバーレイのグリッド密度上限を修正：レート制限の計算前提が誤っていたことが判明（3,500→500点）**（2026-08-xx）— 前項の同時実行数対策後もユーザーから「Minutely制限がずっと出る」との報告。調査したところ、固定エリア方式の密度上限（3,500点）を決めた際の私（Claude）自身の計算に誤りがあったことが判明——「1 HTTPリクエスト（1バッチ）＝1コール」と誤って前提していたが、Open-Meteo開発者本人のGitHubコメント（[open-meteo/open-meteo#1295](https://github.com/open-meteo/open-meteo/issues/1295)）によれば「複数地点を指定したクエリは、1地点あたりの重みに地点数を掛けた分だけカウントされる」——つまり100地点のバッチは約1コールではなく約100コール分として数えられる。正しい重み計算式（開発者コメントより）：`max(1, 変数数÷10) × max(1, 予報日数÷7) × 地点数`。本機能は変数3つ（変数側の係数は1）・forecastDays最大8日（日数側の係数は約1.14）のため、3,500点では1クリックあたり約4,000コール分——Minutely上限（600）を7倍近く超えていた（同時実行数対策はサーバーへの同時アクセス数を抑えるだけで、この地点数で重み付けされるコール数上限には無関係だったため、前回の修正では解決しなかった）。**修正**：`Minutely（600）÷ 予報日数8÷7 ≈ 525点`が理論上の上限と逆算し、約5%のマージンを見て`waveFieldFixedAreaMaxPoints`を3,500→500に変更（`wave_field_cache.dart`）。この結果、固定エリア（500平方度）での間隔は約0.4度→約1.0度まで粗くなる——広いエリアをこの密度で持たせようとすると、無料枠の重み付け上限では原理的にこれ以上は無理という結論。確認済み：文字列・コメントを除外した専用スクリプトで丸括弧・波括弧・角括弧の対応が一致。単一定数の変更（doc コメントのみ大幅書き換え）のためAgentレビューは省略。詳細な計算過程は`docs/devlog-online-xml.md`「レート制限の計算前提の誤り（地点数による重み付け）」参照。

**波の場オーバーレイで「Too many concurrent requests」エラーの原因を特定・修正（同時実行数の制限）**（2026-08-xx）— 固定エリア方式へ再設計後、ユーザーから実機で「Too many concurrent requests」ばかり出るとの報告。分/時間/日のコール数上限とは別物か、サーバー混雑なのかを調査したところ、Open-Meteo開発者本人のGitHubコメント（[open-meteo/open-meteo#1493](https://github.com/open-meteo/open-meteo/issues/1493)）で「無料枠は1IPあたり同時に1リクエストしか処理せず、追加は最大6件までキュー、7件目以降は`Too many concurrent requests`で拒否」というサーバー側の同時実行数制限であることが判明（コール数の上限とは完全に別の仕組み）。`fetchOpenMeteoMarineGrid`（`open_meteo_marine_fetcher.dart`）は全バッチ（固定エリア方式では約35バッチ）を`Future.wait`で一斉発火する設計だったため、この7件という上限を即座に超えていたことが原因と特定。**修正**：同時実行数を4に制限する固定サイズのワーカープール方式（新規`_maxConcurrentBatches`定数・`_runBatchesWithLimitedConcurrency`関数）に変更。各ワーカーは共有カウンタから次のバッチ番号を取得して処理し、無くなるまで繰り返す（Dartはシングルスレッド協調的スケジューリングのため、カウンタの読み取り・インクリメント間に`await`が無ければロック不要で安全）。Agentツール（general-purpose）による独立レビューを実施——ワーカープールの正しさ・結果の順序保持・エラー伝播・エッジケース（バッチ0件／1〜3件／ちょうど4件）はいずれも問題なしと確認、ただし軽微な指摘1件（1バッチが失敗しても他のワーカーが新規バッチの取得を止めずに続けてしまい、原因が同時実行数上限そのものだった場合は失敗直後も上限に当たり続けてしまう）を受け、失敗検知後は新規バッチの取得を止める（進行中のリクエストは自然完了に任せる）よう修正。確認済み：文字列・コメントを除外した専用スクリプトで丸括弧・波括弧・角括弧の対応が一致。詳細は`docs/data-format-notes.md`「Too many concurrent requests」参照。Coworkサンドボックス（Dart SDKなし）のためコンパイル確認は未実施——Windows機での実機確認が必要（TASKS.md参照）。

**波の場オーバーレイを、地図連動の動的取得から固定エリア・手動Import方式へ全面再設計**（2026-07-30）— 静止時間延長・自動継続廃止（次項）を行ってもなお、動的ビューポート方式そのものの複雑さ・予測しづらさが根本課題として残ると判断したユーザーから「①25N-45N,125E-150Eの固定エリア、②ズームしてもアウトしても再取得しない（エリア内はコール済みデータのみ表示）、③1時間TTLの自動失効もなくす、④ユーザーがクリックした時のみ取得、⑤キャッシュは次回コールまで残す」という抜本的な簡略化案の相談。地図のパン・ズーム追従・タイル分割・デバウンス・クールダウン・自動継続といった同日実装した仕組み一式が丸ごと不要になる設計転換のため、実装前に方針を確認（AskUserQuestion）：①取得トリガーのUI形状（Display+Importを分離／現行の1ボタン兼用）→**JMA/JTWCと同じ「Display（表示切替）＋Import（取得）」に分離**を選択、②固定エリア用のグリッド密度上限（従来の900のままか、より高精度か）→「クリック時のみの取得なのでレート制限の心配はない、1時間に1度取得しても上限に達しない程度」という制約で一任され、実測（Daily上限10,000に対し1クリック約35リクエスト×1時間おき24回でも約840/日と十分余裕）を踏まえ3,500（間隔約0.4度、旧タイル設計と同等の密度）に設定。**実装**：`wave_field_cache.dart`を全面書き換え——タイル・TTL・クールダウン関連の型・定数・関数をすべて削除し、`WaveFieldSavedState`（displayEnabled・points・fetchedAtのみ）＋`loadWaveFieldCache`/`saveWaveFieldCache`という単純な単一エントリ永続化に縮小、固定エリア座標（25N-45N/125E-150E）と密度上限（3,500）を定数化。`map_screen.dart`：AppBarの波の場トグルボタンを削除し、Information dialog（`_showLabelSettingsDialog`）内にMarine Forecastと同じ構成の「Wave Field」セクション（Display チェックボックス＋「Import wave field」ボタン、エラー表示、「Cached: ...」表示）を新設。取得処理はダイアログ内ローカルクロージャ`importWaveField`として実装（JMA/Marine Forecastの`fetchMarineForecast`と同じ`dialogOpen`ガード・パターン）し、Saveボタンを押すまでは実際の状態に反映されない（Cancel時は破棄、他セクションと同じ挙動）。表示のOn/Off連動のストリークアニメーションタイマーは新設`_syncWaveAnimTimer()`で管理。旧方式のビューポート追従・タイル・世代カウンタ・デバウンス・クールダウン関連のフィールド・メソッドはすべて削除（`_setWaveFieldEnabled`／`_onViewportChangedForWaveField`／`_visibleLatLonBounds`／`_fetchWaveFieldGrid`／`_fetchOneWaveFieldTile`ほか）。Agentツール（general-purpose）による独立レビューを実施——残存参照（削除した識別子への言及）・ダイアログのライフサイクル（`dialogOpen`ガード・ボタンの無効化解除漏れ）・Save/Cancel時の挙動・アニメーションタイマー同期・JSON永続化のnull安全性、いずれも重大な指摘なし。軽微な指摘1件（`wave_field.dart`冒頭のdocコメントが削除済みの`_fetchWaveFieldGrid`/`_visibleLatLonBounds`を参照したまま）を反映済み。確認済み：変更2ファイルとも文字列・コメントを除外した専用スクリプトで丸括弧・波括弧・角括弧の対応が一致。Coworkサンドボックス（Dart SDKなし）のためコンパイル確認は未実施——Windows機での実機確認が必要（TASKS.md参照）。

**波の場オーバーレイでOpen-Meteoの日次上限に到達、バックグラウンド自動継続取得を廃止・静止時間を0.7秒→3秒に延長**（2026-07-30）— タイルキャッシュ実装（次項）を実機テストしたユーザーから「Dailyの上限を超えてしまった」と報告。原因は前項の実装に含めていた「1ラウンドで新規取得タイル数の上限（6タイル）を超えた残りは、ユーザー操作なしでもクールダウン後に自動で次ラウンドを継続する」仕様——ズームアウトしたビューを放置するだけでも裏で連続的に実通信し続ける可能性があった。ユーザーに診断を提示したところ「廃止する」との判断。`map_screen.dart`の`_fetchWaveFieldGrid`から自動継続タイマーの予約処理を削除——1ラウンドの上限を超えた残りタイルは、ユーザーが再度パン/ズームして静止するか、オーバーレイをOff→Onし直すまで取得されないままになる（このプロジェクトの他の取得箇所と同じ「自動取得より手動トリガー優先」方針、CLAUDE.mdの「取得方式」参照）。あわせてパン/ズーム停止から実取得までの静止時間（デバウンス）を0.7秒から3秒に延長（ユーザー選択、選択肢0.7/2/5秒のうち「3秒」を指定）——探索中の一瞬の間でも取得が発動しにくくする。実通信自体のクールダウン（5秒に1回まで）・1ラウンドの新規取得タイル数上限（6タイル）は変更なし。単一メソッドからの削除的変更・定数変更のみのためAgentレビューは省略、丸括弧・波括弧・角括弧の対応チェックのみ実施（一致）。詳細は`docs/devlog-online-xml.md`「波の場タイルキャッシュへの再設計」参照。

**波の場オーバーレイのキャッシュをボックス単位からタイル単位に作り直し、新規取得タイル数の上限・通信クールダウンを追加**（2026-07-30）— ユーザーから「パン後にキャッシュがどれくらい効くのか実感が薄い」との指摘を受け、ボックス単位キャッシュ（前項）の実効性を検証したところ、パディング付きボックスの完全内包判定は「ズレ量0（全く同じ表示）」以外ではほぼ常に不成立になる（数式で確認、詳細下記devlog）という設計上の欠陥が判明。マップを`waveFieldTileSizeDeg`（8度、5/8/12度の3択からユーザー選択）四方の固定タイルに分割し、タイルIDの一致だけで判定する方式に作り直し（`wave_field_cache.dart`を全面書き換え：`WaveFieldTileId`／`waveFieldTilesOverlapping`／`waveFieldTileBounds`／`findFreshWaveFieldTile`、キャッシュエントリはタイルIDをキーに1タイル1エントリで重複排除）。あわせてユーザーが選んだ2つの追加ガードを実装：①1回のパン/ズームで新規取得するタイル数を`maxNewWaveFieldTilesPerFetch`（6タイル、画面中心に近い順）に制限、②実際のネットワーク取得（キャッシュヒットは対象外）を`waveFieldFetchCooldown`（5秒）でクールダウン。`map_screen.dart`の`_fetchWaveFieldGrid`を全面書き換え——必要タイルを算出→フレッシュなキャッシュタイルは即座に表示（部分表示を許容）→残りのうち上限までを画面中心優先で選び、クールダウン中なら残り時間分待って自動リトライ、なければ取得。Agentツール（general-purpose）による独立レビューを2回実施し、1回目で重大な指摘3件を修正：①`fetchOpenMeteoMarineGrid`はレスポンスの並び順・件数が入力と一致する保証がない（各結果は要求座標ではなくモデルの格子中心座標を返す）とドキュメントに明記されているにもかかわらず、複数タイル分をまとめて1リクエストにし件数ベースでスライスして各タイルに割り当てていた→タイルごとに個別の`fetchOpenMeteoMarineGrid`呼び出し（`_fetchOneWaveFieldTile`を新設、`Future.wait`で並列化）に変更しタイル間の突き合わせを完全排除、②新しい取得に追い越された（世代カウンタ不一致）取得済みデータがキャッシュへの書き込み前に破棄されており、既に消費したAPIコール分が無駄になっていた→キャッシュへの保存を世代チェックより前に移動、③タイル境界ちょうどにボックス端が一致する場合の1タイル分の過剰取得（境界値エッジケース）→終端インデックス計算にごく小さいイプシロンを導入。2回目のレビューで3件とも修正が正しいことを確認、加えて「オーバーレイ再有効化時に前回途中で終わったタイルを続きから埋める」ため`_setWaveFieldEnabled`の再取得条件を「グリッドが空の時のみ」から「常に試行（キャッシュヒットのみなら実質無料）」に変更。確認済み：変更2ファイルとも文字列・コメントを除外した専用スクリプトで丸括弧・波括弧・角括弧の対応が一致。詳細な数式による検証・トレードオフの説明は`docs/devlog-online-xml.md`「波の場タイルキャッシュへの再設計」参照。Coworkサンドボックス（Dart SDKなし）のためコンパイル確認は未実施——Windows機での実機確認が必要（TASKS.md参照）。

**波の場オーバーレイに1時間TTLのディスクキャッシュを実装、レート制限対策として採用**（2026-07-30）— 取得範囲の絞り込みでは効果が薄いと判明した後、ユーザーから「1/2組み合わせ（自動追従＋キャッシュ）が良い、1時間で十分、アプリ再起動後も再取得せず復元したい」との要望を受け実装。新規`lib/utils/wave_field_cache.dart`（`WaveFieldCacheEntry`：取得ボックス＋取得時刻＋forecastDays＋グリッド点、`covers`/`isFreshAt`で判定、`loadWaveFieldCache`/`saveWaveFieldCache`でapplication supportディレクトリのJSONファイルに永続化）。キャッシュ粒度はポイント単位ではなくボックス単位（`buildWaveFieldGridPoints`のcoarseningでボックスサイズごとに間隔が変わり、ポイント単位だと異なるズームレベル間で座標が揃わないため簡易化した設計判断）。`map_screen.dart`の`_fetchWaveFieldGrid`に統合：必要なボックスを計算した後、`_waveFieldCache`に「fresh・forecastDays十分・covers」を満たすエントリがあれば即座にそれを表示しネットワーク取得をスキップ、無ければ従来通り取得しキャッシュへ追記・永続化。Agentツール（general-purpose）による独立レビューを実施し、reject 2件を反映：①キャッシュ判定が地理範囲のみでforecastDaysを見ておらず、再生時間が後から伸びた場合に古い（短い）系列を再利用して「色が変わらない」バグを再発しうる問題→`forecastDays`をエントリに追加し判定に組み込み、②in-memoryの`_waveFieldCache`がTTLフィルタのみで件数上限がなく、まさにこのキャッシュが対処しようとしている「短時間の連続パン・ズーム」でメモリが際限なく増えうる問題→`saveWaveFieldCache`とin-memory更新の両方で使う共通関数`pruneWaveFieldCache`（TTLフィルタ＋件数上限30の両方）に統一。軽微な指摘1件（起動時のディスク読み込みとフェッチ完了が競合すると片方が上書きされうる）も、置き換えでなくマージする方式に修正して反映。確認済み：新規1ファイル・変更1ファイルとも文字列・コメントを除外した専用スクリプトで丸括弧・波括弧・角括弧の対応が一致。Coworkサンドボックス（Dart SDKなし）のためコンパイル確認は未実施——Windows機での実機確認が必要（TASKS.md参照）。

**波の場オーバーレイのレート制限対策（取得範囲の絞り込み）を検討し、現状維持で保留**（2026-07-30）— HTTP 414修正後、開発中の連続パン・ズームで`Minutely`（分間600コール）・`Hourly`（時間5,000コール）の両レート制限に到達したことを受け、対策として固定範囲への絞り込み（ユーザー提案の2案）を試算。いずれも現行の`maxPoints=900`キャップが先に効くため、バッチ数（7〜9）はほぼ現状と変わらないと判明（絞り込みが効果を持つのは面積144平方度以下からで、提案の2案はいずれもそれより大幅に広かった）。ユーザー判断により「変わらないなら現行のままで良い」と結論、検討過程で実装しかけていた範囲キャップ（`_waveFieldMaxBoxSpanDeg`、ユーザー未承認のまま実装してしまっていたため差し戻し済み）は`map_screen.dart`から削除。zip配布ビルドでの通常利用は問題なし、レート制限は主に開発中の連続テスト操作で発生する事象と整理。将来の対策候補は`TASKS.md`「決定待ち・要確認事項」に記録。確認済み：`map_screen.dart`の丸括弧・波括弧・角括弧の対応が差し戻し前後で一致（差し戻し後のカウントが414修正直後の状態と同一であることを確認）。

**波の場オーバーレイでmaxPoints 900化により発生したHTTP 414「URI Too Long」を修正**（2026-07-30）— 前項の対応（maxPoints 120→900）を実機で試したユーザーから、Open-Meteoへのリクエストが`HTTP 414 (URI Too Long)`で失敗するスクリーンショット報告。原因は900地点をカンマ区切りで1本のURLにまとめて送っていたため、URL長がOpen-Meteoのサーバー（またはその手前のプロキシ）の許容長を超えたこと（公式ドキュメントに明記された制限ではなく実地で判明）。`fetchOpenMeteoMarineGrid`（`open_meteo_marine_fetcher.dart`）を、1リクエストあたり最大100地点のバッチに分割し`Future.wait`で並列取得・結合する方式に変更（新規`_fetchOpenMeteoMarineBatch`）。1点だけのバッチ（Open-Meteoのレスポンス形式がカンマの有無で単一オブジェクト/配列に切り替わる既知の分岐）も内部で正しく処理。Agentツール（general-purpose）による独立レビューを実施——チャンク境界のオフバイワン・forecastDaysの受け渡し・Future.waitの失敗時挙動（全バッチ完了を待ってから最初のエラーを投げる、部分成功データは返さない）・並行リクエスト間の共有状態、いずれも重大な指摘なし。軽微な指摘2点（1点バッチのみ小数点精度が4桁で他バッチの3桁と不揃い、レスポンス配列要素の型キャストが失敗時に独自例外ではなく生の`TypeError`を投げる）はいずれも既存コードから引き継いだ軽微な不整合のため今回は見送り。詳細は`docs/devlog-online-xml.md`「グリッド密度不足の修正」参照。確認済み：`open_meteo_marine_fetcher.dart`の丸括弧・波括弧・角括弧の対応が一致。Coworkサンドボックス（Dart SDKなし）のためコンパイル確認は未実施——Windows機での実機確認が必要（TASKS.md参照）。

**波の場オーバーレイのグリッド密度不足を修正（maxPoints 120→900）**（2026-07-30）— ビューポート方式への変更後、ユーザーが実機で「全範囲は網羅されたが波浪情報として使えないレベルに粗い」と報告。原因は表示面積が航路周辺のみの頃より大幅に広がった（ズームアウト時は最大85×45度）にもかかわらずグリッド上限点数（120点）を据え置いていたため。Open-Meteoのマルチロケーションエンドポイントが1コールあたり最大1000地点までサポートすると公式ドキュメントで確認し、安全マージンを残した900に引き上げ（`wave_field.dart`の`buildWaveFieldGridPoints`）。データ量・描画負荷の増加はユーザーが承知の上での要望。詳細・計算根拠は`docs/devlog-online-xml.md`「グリッド密度不足の修正」参照。確認済み：変更2ファイル（`wave_field.dart`／`open_meteo_marine_fetcher.dart`）とも文字列・コメントを除外した専用スクリプトで丸括弧・波括弧・角括弧の対応が一致。単一定数＋docコメントの変更のためAgentレビューは省略。Coworkサンドボックス（Dart SDKなし）のためコンパイル確認は未実施——Windows機での実機確認（密度・描画負荷とも）が必要（TASKS.md参照）。

**Open-Meteo波の場オーバーレイの表示範囲を「航路周辺のみ」から「地図の現在の表示範囲」方式へ変更**（2026-07-30）— 2026-07-29に合意していた方針（TASKS.md参照）を実装。`_fetchWaveFieldGrid`の境界計算を現在のビューポート（`_transformationController`＋既存の`_latLonAtViewportPoint`で左上・右下を逆変換）ベースに変更し、可視範囲サイズの15%をパディング、パン・ズームは`_transformationController`のリスナー経由で700msデバウンスして再取得、応答の順序保証がないため世代カウンタ（`_waveFieldFetchGeneration`）で古い応答の上書きを防止。Agentツール（general-purpose）による独立レビューを実施——重大な指摘なし。軽微な指摘3点（`_setWaveFieldEnabled`の二重登録防止の早期リターン、無効化時の`_waveFieldLoading`防御的リセット、fire-and-forget呼び出し2箇所へのコメント追加）を反映。詳細・設計判断の経緯は`docs/devlog-online-xml.md`「②の実装（2026-07-30）」参照。確認済み：変更3ファイル（`map_screen.dart`／`wave_field.dart`／`open_meteo_marine_fetcher.dart`）とも文字列・コメントを除外した専用スクリプトで丸括弧・波括弧・角括弧の対応が一致。Coworkサンドボックス（Dart SDKなし）のためコンパイル確認は未実施——Windows機での実機確認が必要（TASKS.md参照）。

**地方海上予報（VPCY51）の本番表示を実装、①メインへの採用を決定**（2026-07-30）— ユーザーがWindows実機で「Marine forecast (debug)」ボタンにより`regular.xml`からの実データ取得・パースを確認できたとの報告を受け、本番機能を実装。`MapPainter`に`MarineForecastAreaPolygon`（区域境界ポリゴン＋Base波高、汎用型でJMA固有の型を知らない設計——`WaveFieldSample`と同じ流儀）と`_drawMarineForecastAreas`（区域ごとの塗り分け描画、`_drawGrid`と`_drawWaveField`の間、`_drawCoastline`より前に描画し陸地でマスクされるようにする）を追加。色スケールは0〜6mの5段階（個人用Open-Meteoの0〜4mスケールとは別に新設——VPCY51は全国規模のため台風時により高い波高値になりうる想定）。`map_screen.dart`のInformationダイアログに「Marine Forecast (JMA regional sea areas)」セクションを新設（Ship's Nameの直後、Import Allセクションの前）——Display切替・「Import from JMA」ボタン（`fetchLatestMarineForecast`を呼び既存の台風フェッチと同じ`dialogOpen`ガード方式）・エラー表示・「Cached: ...」表示。取得結果（成功した電文のみ、複数官署分をまとめて）はオフラインキャッシュとして永続化（`AppStateStorage`にトップレベルの`marineForecastRawXmls`/`marineForecastFetchedAtJst`/`marineForecastDisplayEnabled`を追加、台風スロットと違い全国1レイヤーのためスロット単位ではなくトップレベルに配置）。Agentツール（general-purpose）による独立レビューを実施し、中程度の指摘1件を反映：`MapPainter.shouldRepaint`の変更検知が区域の並び順（`Map.entries`の反復順、官署ごとの発表順が変わると入れ替わりうる）に依存したリスト位置比較になっており、同じ件数でも区域の入れ替わりで検知漏れ・誤検知が起こり得た。`MarineForecastAreaPolygon`に汎用の識別子`id`（呼び出し側はArea/Codeを渡すが、painter自体はJMA固有の意味を知らない）を追加し、区域コードで一致させてから高さを比較する方式に修正。**未実装（意図的なスコープ縮小）**：`Becoming`（変化点）への再生スライダー連動（「階段状」表示、`docs/devlog-online-xml.md`の当初設計）は、`TimeModifier`（例「１０日２１時までに」という自由文言）を絶対時刻へ解析するロジックが実データでの文言パターン未確認のため見送り、常にBase（現況）値を表示する（詳細・今後の着手条件は`jma_marine_xml_parser.dart`の`MarineWaveBecoming`docコメントとTASKS.md参照）。確認済み：3ファイルとも文字列・コメントを除外した専用スクリプトで丸括弧・波括弧・角括弧の対応が一致。Coworkサンドボックス（Dart SDKなし）のためコンパイル確認は未実施——Windows機での実機確認が必要（TASKS.md参照）。

**波の場オーバーレイが再生中に色が変化しないバグを修正**（2026-07-29）— ユーザー報告「再生から最後まで色が変わっていかない」を受けて調査。原因：`_fetchWaveFieldGrid`がOpen-Meteoへの取得日数（`forecast_days`）を既定値2日のまま呼んでいたため、航海計画の再生時間（数日規模になりうる）の途中で取得済みデータの末尾に達し、以降は`interpolateHourlyValue`のクランプ処理により最後の時刻の値に固定されて見えていた（Open-Meteoの時系列が船の`_startTime`ではなく実際の「今日」を起点にする点も影響）。再生終了時刻（`_startTime`＋`_maxOffsetHours`）から逆算し、実時間での日数差＋1日の安全マージンを、API上限の8日までに収めて`forecastDays`に渡すよう修正。修正中に`num.clamp`が`int`ではなく`num`を返す既知の型エラー（過去にも本プロジェクトで発生、2026-07-26の地図画面実装時と同種）に気付き、`math.max`/`math.min`の組み合わせに置き換えて回避。確認済み：`map_screen.dart`の丸括弧・波括弧・角括弧の対応が一致。Coworkサンドボックス（Dart SDKなし）のためコンパイル確認は未実施——Windows機での再ビルド・実機確認が必要。

---

**Open-Meteo波の場オーバーレイのユーザーレビュー・今後の方針決定**（2026-07-29）— ユーザーが実機で波の場オーバーレイを確認し「本当に良い、船長として役立つ」と高評価、個人用として採用を確定。あわせて①メイン（VPCY51）と②個人用（Open-Meteo）は独立した判断であること、②の表示範囲を「航路周辺のみ」から「画面に表示中の範囲だけを取得」する方式（パン・ズームで再取得）に変更する方針を決定（全エリア一括取得はデータ量・描画負荷・見た目の粗さの3点でデメリットが大きいことを説明した上での選択）。実装は次回セッション着手（TASKS.md参照）。詳細・検討した3案の内容は`docs/devlog-online-xml.md`「Open-Meteo個人用試用機能・波の場オーバーレイの実装と今後の方針」参照。

**Open-Meteo波の場（波高）を地図上にオーバーレイ表示する機能を実装**（2026-07-29）— 前項の試用ダイアログで実際に波高データが取れることをユーザーが確認したのを受け、「地図上に色分け表示、Windyのようなタイムバー連動アニメーションはできる？」との質問・依頼。実装方針をAskUserQuestionでユーザーに確認：①表示スタイル（シンプルなグリッド色分け／Windy風アニメーション）→**Windy風アニメーションを選択**、②取得範囲（航路周辺のみ／表示中の地図全体）→**航路周辺のみを選択**。Windy本家はGPUシェーダー・パーティクルシステムだが、今のアプリはCustomPainterの自前描画のため完全再現は別物の作り込みになる旨を事前に説明した上で、簡易版（ぼかし円による色分けヒートマップ＋波向きに流れる短いストリークのアニメーション）として実装する方針で進めた。実装：①`open_meteo_marine_fetcher.dart`に複数地点バッチ取得`fetchOpenMeteoMarineGrid`を追加（単一地点取得とパース処理を`_parseSingleLocationResult`に共通化）、②`wave_field.dart`新設（航路のbounding boxから点数上限付きのグリッド生成`buildWaveFieldGridPoints`、時刻補間`interpolateHourlyValue`——波向きは360°境界をまたぐため通常の線形補間ではなく短い方向に補間する専用ロジック）、③`map_painter.dart`に汎用的な`WaveFieldSample`型と`_drawWaveField`（ぼかし円の色分け＋`_drawWaveStreaks`の流れるアニメーション）を追加——MapPainter自体はOpen-Meteo固有の型を一切知らない設計を維持、④`map_screen.dart`にAppBarトグル（kPersonalBuild限定）・航路周辺bboxの算出・アニメーション用Timer（再生バーとは独立した実時間ベース、Onの間のみ動作）を実装。Agentツール（general-purpose）による独立レビューを実施——4ファイルとも重大な指摘なしだったが、軽微な潜在バグ2点を検出：①グリッド点数の上限が極端な入力（非常に広い範囲×非常に小さい上限）では守られない可能性、②複数地点バッチAPIに地点数1つだけを渡すとOpen-Meteo側のレスポンス形式（配列か単一オブジェクトか）判定を誤る可能性。いずれも現在の実際の呼び出し方（航路周辺は必ず複数点、上限120点に対し最悪ケースでも32点程度）では発生しないことをレビューで確認済みだが、将来の呼び出し方変化に備えて両方とも修正済み（グリッド生成後に上限で切り詰め、地点数1の場合は単一地点用の関数にそのまま委譲）。確認済み：新規2ファイル・変更2ファイルとも文字列・コメントを除外した専用スクリプトで丸括弧・波括弧・角括弧の対応が一致。Coworkサンドボックス（Dart SDKなし）のためコンパイル確認は未実施。**次の一手はユーザーによるWindows実機確認**（TASKS.md参照）：`run_windows_personal.bat`で起動し、AppBar新アイコンで波の場オーバーレイの見た目（色・アニメーション）を確認、方向性を判断してもらう。

**Open-Meteo Marine Weather APIの個人用試用機能を実装**（2026-07-29）— ユーザーより「VPCY51が実データで確認しづらいので、個人用として計画していたOpen-Meteoを試したい」との依頼。`docs/devlog-online-xml.md`「本流／個人の分岐方法（決定）」の合意通り、gitブランチではなく`--dart-define`のコンパイル時フラグで分岐。`lib/utils/build_flags.dart`を新設し`kPersonalBuild`（`bool.fromEnvironment('PERSONAL_BUILD')`）を定義、`lib/utils/open_meteo_marine_fetcher.dart`でOpen-Meteo Marine Weather API（`https://marine-api.open-meteo.com/v1/marine`、緯度経度指定で1時間刻みの連続時系列——波高／波向／周期）を取得・パース。時刻は`DateTime.parse`を使わず正規表現で手動パース（過去の「再生バーの日付ずれバグ」と同種の落とし穴を事前回避）。`map_screen.dart`に`kPersonalBuild`時のみ表示される試用ダイアログ（船の現在位置または既定地点の緯度経度で取得・一覧表示、DWD/Open-Meteoへの出典表示込み）を追加。起動・ビルド用に`run_windows_personal.bat`／`build_release_personal.bat`を新設（`.bat`納品前チェックリスト——CRLF変換・括弧ブロックなし・日本語直書きなし——確認済み）。本流の`run_windows.bat`／`build_release.bat`は無変更。Agentツール（general-purpose）による独立レビューを2回実施（VPCY51機能と共通のdialogOpenガードパターン、コンパイル時フラグによるデッドコード除去の妥当性、タイムスタンプ解析の正しさ、コントローラのdispose漏れ、JSON解析時の例外がダイアログ側で握りつぶされずに表示されるか、を確認）——いずれも重大な指摘なし。確認済み：新規3ファイル・変更1ファイルとも文字列・コメントを除外した専用スクリプトで丸括弧・波括弧・角括弧の対応が一致。**次の一手はユーザーによるWindows実機確認**（TASKS.md参照）：`run_windows_personal.bat`で起動し試用ダイアログで実データを見た上で、この機能を本採用するか見送るか判断してもらう（見送る場合は関連ファイル一式を削除するだけで済む設計）。

**VPCY51実データ検証用の「Marine forecast (debug)」機能を実装**（2026-07-29）— TASKS.mdの最優先タスク「regular.xmlの実データ裏取り・パーサー確認」の着手。`lib/utils/jma_marine_feed_fetcher.dart`を新規実装（`jma_feed_fetcher.dart`のVPTW60取得パターンを踏襲、`regular.xml`から「地方海上予報」を含む全エントリのURLを取得——地方毎に複数官署が発表しうるため単一件ではなく全件を対象）。各電文を取得し既存の`parseJmaMarineXml`でパース、ダウンロードは成功したがパースに失敗した電文は例外を握りつぶさず`parseError`として結果に残す設計（Coworkからは検証できなかったパーサーの実データ不整合をそのまま可視化するのが目的のため）。`map_screen.dart`のAppBarに検証専用の「Marine forecast (debug)」ボタン（波アイコン）を追加し、取得した区域コード・区域名・期間ラベル・波高（Base/Becoming）をテキストでそのまま一覧表示するダイアログを実装（地図描画・永続化には未接続、あくまで人間が実データと見比べるための一時的な検証UI）。Agentツール（general-purpose）による独立レビューを実施し、重大な指摘1件を反映：新規ダイアログに`_showLabelSettingsDialog`と同種の`dialogOpen`ガード・`barrierDismissible: false`が欠けており、フェッチ中にダイアログを閉じると破棄済みダイアログへ`setDialogState`が呼ばれうる不具合を修正。あわせて軽微な指摘（パーサーの未知の例外型がダイアログ側・フェッチ側どちらでも捕捉されず`loading`が戻らなくなるケース）も両ファイルに`catch`を追加して解消。確認済み：新規ファイル・変更ファイルとも文字列・コメントを除外した専用スクリプトで丸括弧・波括弧・角括弧の対応が一致。Coworkサンドボックス（Dart SDKなし）のためコンパイル確認は未実施。**次の一手はユーザーによるWindows実機確認**（TASKS.md参照）：アプリを起動しこのボタンで実際に`regular.xml`を取得、区域・波高の表示内容が気象庁の実発表と一致するか、パースエラーが出ないかを確認してもらう。

**VPCY51電文のDartパーサーを実装**（2026-07-29）— 気象庁公式PDFの記載（`docs/data-format-notes.md`参照、実データでの裏取りは未実施）に基づき`lib/utils/jma_marine_xml_parser.dart`を実装。Agentツール（general-purpose）による独立レビューで実バグを1件発見・修正——Area要素の探索方向が誤っており（Property配下ではなくItem配下から探すべきところをProperty配下で探していた）、実データ投入時に常に空リストが返る状態だった。例外を投げない設計のため気付きにくいバグで、他にも同種のバグが無いか今後の実データ確認時に注意が必要（教訓は`docs/devlog-online-xml.md`参照）。`MapPainter`への塗り分け描画・実データ確認は次のステップ（TASKS.md参照）。確認済み：文字列・コメントを除外した専用スクリプトで括弧の対応が一致、修正後に全文読み直しで変数定義漏れが無いことを確認。Coworkサンドボックス（Dart SDKなし）のためコンパイル確認は未実施。

**地方海上予報（VPCY51）の予報区ポリゴン取得・簡略化・Dart読み込みコードを実装**（2026-07-29）— ユーザーがWindows機で`download_marine_areas.ps1`を実行し、NII Geoshapeから全48区域の生GeoJSON（合計約213MB）を取得。CoworkサンドボックスでPython+Shapelyにより簡略化・結合し、`assets/marine_areas/marine_areas.json`（447KB）を生成、`pubspec.yaml`に登録。読み込みコード（`lib/utils/marine_areas.dart`）と区域名定数（`lib/utils/marine_area_codes.dart`、48区域とも`marine_areas.json`のキーと一致確認済み）を実装。VPCY51電文のDartパーサー・`MapPainter`への塗り分け描画は次のステップ（TASKS.md参照）。技術的な詳細（213MBの発見経緯、簡略化手法、Coworkのbashツールの45秒タイムアウト制約への対処）は`docs/devlog-online-xml.md`「予報区GeoJSONの取得・簡略化」参照。確認済み：新規Dartファイル2件・`marine_areas.json`のキー48件が`marine_area_codes.dart`の定数48件と完全一致、括弧の対応も一致。Coworkサンドボックス（Dart SDKなし）のためコンパイル確認は未実施——次回Windows実機でのビルド確認が必要。

**海上気象データ追加フェーズ着手、JMA地方海上予報(VPCY51)のスキーマ調査完了**（2026-07-29）— オンライン化フェーズ①完了を受け、「今後の開発方針」で合意していた次の実装対象（海上気象データソースの追加）に着手。ユーザー選択により①沿岸波浪予報（JMA地方海上予報）から着手することを確認。Agentツールによる独立調査で、`docs/devlog-online-xml.md`時点の想定のうち2点が実態と異なっていたことが判明：①期間ブロックは「本日/明日等の固定ブロック」ではなく`Base`＋`Becoming`（変化点方式）だった、②波の高さは気象庁「海上分布予報」の階級文字列ではなく実数値（メートル）そのものだった。また、地方海上予報区の最南が北緯約24°までであり、アプリ表示範囲南側（フィリピン近海、北緯5〜19°）は予報区の対象外であることが判明（TASKS.mdに決定待ち事項として追加）。予報区ポリゴンデータはNII Geoshape（`https://geoshape.ex.nii.ac.jp/jma/resource/AreaMarineAJ/`）で区域コードごとに個別GeoJSON取得可能と確認、海岸線データより軽量に入手できる見込み。実際に配信中のVPCY51電文本体の取得はネットワーク制限のため未達成——実装時に6/12/18/24時JST前後で`regular.xml`を再取得し裏取りする必要がある。詳細は`docs/data-format-notes.md`「気象庁 防災情報XML（VPCY51、地方海上予報）」・`docs/devlog-online-xml.md`「実データ調査による更新」参照。

**Clearボタンを押してもJTWC Displayチェックボックスがオンのまま残るバグを修正**（2026-07-29）— ユーザー報告「JMAインポート後、Clearを押してもDisplayチェックボックスがOffにならない（JTWCはそうなっている）」を受けて`_showLabelSettingsDialog`のClearボタンのハンドラを確認したところ、コード上はJMA Display（`jmaDisplayLocal[i] = false`）は既に正しくリセットされる一方、JTWC Display（`jtwcDisplayLocal[i]`）はハンドラ内で一度も触れられておらず、ボタン自身のコメント「Clear reliably means "start this slot over" for either source」に反して**JTWC側だけリセット漏れ**になっていたことが判明（ユーザー報告とは逆方向の非対称だが、いずれにせよ「Clearで両ソースとも確実にOffにしたい」という要望自体は満たされていなかった）。`jtwcDisplayLocal[i] = false;`を追加し、両ソースとも確実にOffになるよう修正。単一ファイル・同期処理の1行追加のためAgentレビューは省略、丸括弧・波括弧・角括弧の対応チェックのみ実施（一致、エラーなし）。Windows実機確認済み（2026-07-29、ユーザー確認：「Good」——JMA・JTWCともClearでDisplayチェックボックスがOffになることOK）。

**「Fetch」表記の「Import」への統一・Import All完了メッセージの英語化**（2026-07-29）— 複数台風対応（Fetch Allボタン）の実機確認時のフィードバック2件に対応。①ユーザーから「Fetchという表現をわかりやすくImportに変更してほしい」との要望を受け、UI上のボタンラベル・説明文をすべて「Fetch」→「Import」に変更（Fetch from JMA→Import from JMA、Fetch from JTWC→Import from JTWC、Fetch All (JMA/JTWC)→Import All (JMA/JTWC)、Fetching...→Importing...）。あわせて、変更箇所を指す既存コメント内の引用文言（"Fetch from JMA"等）も古い表記のまま残さないよう更新。Dart側の内部関数・変数名（`fetchJma`／`fetchAllJma`等）はユーザー非表示のため対象外（Passage Plan改称時と同じ方針）。②一括取り込み成功時のメッセージを、日本語の「N件の台風情報をTyphoon 1〜Nに反映しました。」から、ユーザー指定の英語表記「Imported to Typhoon 1」「Imported to Typhoon 1, 2」「Imported to Typhoon 1, 2, 3」に変更（`_importedToSlotsLabel`ヘルパーを新設し、常にスロット1から連番で埋まる仕様を利用して件数から自動生成）。またユーザーからの質問「一括取り込み後もTyphoon 1/2/3それぞれ独立してDisplay On/Off切替できるか」に対し、Import All処理は各スロット既存の`jmaDisplayLocal[i]`/`jtwcDisplayLocal[i]`（個別Fetchボタンや通常のDisplayチェックボックスと共通の状態）を書き換えるだけで、チェックボックス自体は元々スロットごとに独立している設計のため、独立して動作することをコードレビューで確認・回答（実機確認は1件のみの状況だったため保留、TASKS.md参照）。確認済み：`map_screen.dart`の丸括弧・波括弧・角括弧の対応が0で一致。

**複数台風同時発表対応・Fetch Allボタンを実装**（2026-07-29）— TASKS.mdに残っていた「複数台風が同時に発表されている場合の対応」に着手。ユーザー要望「2つ以上ある場合はワンクリックで台風1/2/3に同時入力」を受け、既存の台風1/2/3個別のFetchボタンは残したまま、新規に「Fetch All (JMA)」「Fetch All (JTWC)」ボタンをInformationダイアログ上部（Ship's Name欄の下）に追加。JMA・JTWCで一覧データの性質が異なることが判明したため対応を分けた：①JTWC（RSS）は「現在アクティブな系統」だけがきれいに列挙されており、複数系統は同じ`<description>`内に連結されているだけなので、そのまま全リンクを拾えば重複の心配なく複数系統を取得できる（`jtwc_feed_fetcher.dart`に`fetchActiveJtwcWarningTexts()`を追加）。②JMA（`extra.xml`）は「発表1回＝1エントリ」の一覧であり、同じ台風が3時間おきに出す複数の発表がそのまま並ぶため、候補を実際に取得・パースした上で`JmaTyphoonInfo.eventId`（無ければ号数＋名称、それも無ければ電文URL自体）で重複排除し、異なる台風だけを最大3件抽出する処理が必要だった（`jma_feed_fetcher.dart`に`fetchActiveJmaTyphoons()`を追加、鮮度チェックは追加せず既存の単体取得と同じ「見つかった情報をそのまま信頼する」設計を踏襲）。両関数とも見つかった数だけTyphoon 1から順に反映し、見つからなかった分のスロットは既存データを保持したまま変更しない。詳細は`docs/data-format-notes.md`「複数台風の見分け方」参照。Agentツール（general-purpose）による独立レビューを実施——重大な指摘なし（重複排除ロジック、ダイアログ破棄後のsetState防止ガード、未検出スロットの温存、型整合性いずれも確認済み）。軽微な指摘（リネーム後の関数への旧名でのdocコメント内参照2箇所）は反映済み。確認済み：3ファイルとも文字列・コメントを除外した専用スクリプトで丸括弧・波括弧・角括弧の対応が0で一致。Coworkサンドボックス（Linux、Dart SDKなし）のためビルド未実施——Windows機での実機確認が必要（特に2件以上同時発表されているタイミングでの動作）。

**JTWC自動取得（Fetch from JTWCボタン）を実装**（2026-07-29）— JMA自動取得と同じ理由（データ消費量への配慮）で自動定期取得は行わず、ワンクリックの手動ボタン方式で実装。着手前にブラウザ（Claude in Chrome）でJTWC公式サイトを実際に開き通信内容を調査し、気象庁`extra.xml`に相当する一覧フィード（`https://www.metoc.navy.mil/jtwc/rss/jtwc.rss`、RSS 2.0）を発見。地図範囲（N5-50/E85-170）に該当する系統は`<guid>NWPAC-NIO-WARNINGS</guid>`の項目のみに絞り込めることを確認（詳細は`docs/data-format-notes.md`「JTWC自動取得のURL構造」参照）。`lib/utils/jtwc_feed_fetcher.dart`を新規作成し、一覧フィードから警報テキスト（`***web.txt`）のURLを見つけて取得する`fetchLatestJtwcWarningText()`を実装。JMAの自動取得（`JmaTyphoonInfo`という別系統のパース済みデータを持つ）とは異なり、JTWCはアプリ内に「貼り付けテキスト」という表現が既に1つしか無い設計のため、取得したテキストは新しい状態を持たず既存の貼り付け欄（`typhoonControllers[i]`）にそのまま流し込む設計とした——これにより永続化・再パース・Display連動など既存の仕組みをすべてそのまま再利用でき、JMA側で必要だった新規キャッシュ機構は不要だった。`map_screen.dart`のInformationダイアログに「Fetch from JTWC」ボタンを追加（JTWC Displayチェックボックスと同じ行）。個別の警報テキストファイル（`/jtwc/products/*.txt`）はCowork検証環境からの汎用HTTPクライアントでは空応答だったが実ブラウザでは正常取得できたため、ブラウザ相当のUser-Agentヘッダーを付与する対策を実装（本当に効くかはWindows実機での確認が必要）。Agentツール（general-purpose）による独立レビューを実施——重大な指摘なし。レビューでリンク抽出用正規表現のエスケープに関する指摘（`\.`が非raw文字列中でリテラルのドットとして機能しない可能性）を受けたが、実ファイルを直接確認したところ実際には`\\.`（バックスラッシュを一度エスケープした上でのドット）と正しく書かれておりバグではなかった（レビュー依頼時のプロンプト内サンプルコードの転記ミスが原因と判明）。念のためraw文字列（`r'''...'''`）に書き換えて曖昧さを排除。確認済み：2ファイルとも文字列・コメントを除外した専用スクリプト（raw三重クォート文字列の扱いを本セッションで修正済み）で丸括弧・波括弧・角括弧の対応が0で一致。Windows機での実機確認済み（2026-07-29、ユーザー確認：「Fetch from JTWC」で実際に警報テキストが取得でき、貼り付け欄に反映されることOK）。

**TASKS.mdの陳腐化した項目を削除（台風の実データ接続）**（2026-07-29）— 「台風データの実データ接続をMapScreenに反映」というTASKS.md記載を次の着手候補として検討したところ、記載が参照していた関数`_trackPointsForSlot`が既に存在しないことが判明。2026-07-28のJTWC/JMA同時表示対応時に`_jtwcTrackPointsForSlot`/`_jmaTrackPointsForSlot`へ分割済みで、いずれも最初から実データ（貼り付けたJTWCテキスト・取得したJMA XML）をそのまま読む実装になっており、サンプル/仮データのフォールバックも2026-07-27に廃止済み（台風情報をクリアすると何も表示されなくなるのは意図した動作）。実質的にやることが無い状態だったため、TASKS.mdから当該項目を削除。

**JMA取得結果のオフラインキャッシュ実装、オンライン化フェーズ①のブランチ運用見直し**（2026-07-29）— オンライン化フェーズ①着手にあたりスコープを確認。ユーザーの意向により、①自動定期取得は行わず既存の「Fetch from JMA」手動ボタンのままとする（個人利用でのデータ消費量への配慮）、②取得結果の永続化キャッシュを実装する、の2点で合意。実装：`jma_feed_fetcher.dart`の`fetchLatestJmaTyphoon()`を、パース済み情報のみ返す形から生XML本文も返す`JmaFetchResult{info, rawXml}`に変更。`app_state_storage.dart`の`TyphoonSlotSnapshot`に`jmaRawXml`/`jmaFetchedAtJst`/`jmaDisplayEnabled`/`jmaRingsEnabled`を追加し永続化（JTWC側と同じ「生データを保存し起動時に再パース」方式、`parseJmaTyphoonXml`の将来的な改善が自動的に反映される設計を踏襲）。`map_screen.dart`の`_TyphoonSlot`に`jmaRawXml`/`jmaFetchedAtJst`を追加、`_restoreState`で再パース＋復元（パース失敗時は空扱いにフェイルセーフ）、再生開始時刻の解決優先順位（JMA優先・無ければJTWC）もSaveハンドラと同一ロジックに統一。Informationダイアログには取得結果の下に「Cached: dd Mmm. yyyy HH:MM (JST)」表示を追加し、オフラインで見ているデータがいつ取得されたものかを明示。「現在発表中の台風なし」で取得が空だった場合や取得失敗（オフライン等）時は、既存のキャッシュ表示を消さずそのまま残す設計。あわせて、当初合意していた`feature/online-xml`ブランチでの作業を見送り、main上で直接進める方針に変更（今回のスコープが小粒だったこと、ユーザーの意向）。ブランチ運用は②モバイル対応（`feature/mobile`）着手時から適用。判断の経緯は`docs/devlog-architecture-roadmap.md`「方針の一部見直し」参照。Agentツール（general-purpose）による独立レビューを実施——重大な指摘なし（型/API整合性・非同期フローのdialogOpenガード・「発表なし」分岐でのキャッシュ非消去・DateTime往復のJST規約遵守・優先順位ロジックの一致、いずれも確認済み）。確認済み：3ファイルとも文字列・コメントを除外した専用スクリプトで丸括弧・波括弧・角括弧の対応が0で一致。Windows機での実機確認済み（2026-07-29、ユーザー確認：①Fetch from JMAでの取得、②アプリ再起動後も同じデータが復元表示されること、③Wi-Fi未接続時も前回キャッシュがそのまま表示され続けること、の3点いずれもOK）。

**commit.batの新規ファイル取りこぼしの原因究明・恒久対策**（2026-07-29、`main`ブランチ、commit `1906aae`）— CSVライブラリ機能・JMA自動取得機能のソース本体が`completed-log.md`上は「コミット済み」と記録されていたにも関わらず、実際には一度もpushされていなかった問題を調査。原因は`commit.bat`の`git add`が新規ファイルを手動で個別列挙する方式で、追記漏れが起きると静かに置き去りにする構造だったこと（2026-07-27にも同種の事故が一度起きていたが根本対応せず再発）。`git add -A`への切替、`docs/operation-rules.md`・`docs/flutter-windows-env-notes.md`への再発防止ルール追記で対応。経緯・教訓は`docs/operation-rules.md`「commit.batの新規ファイル取りこぼし」参照。

**プロジェクト運用の土台構築**（2026-07-25）— 5つの宣言（`docs/project-declarations.md`）に基づき、CLAUDE.md／TASKS.md／`docs/operation-rules.md`（本プロジェクト用に書き換え）／`docs/completed-log.md`を整備。技術スタック（Flutter、デスクトップ先行）、セッション構成（単一セッションで進行管理とコーディングを兼務）、Flutterプロジェクトパス（`typhoon_ship_tracker/`、英数字のみ）を決定。ブートストラップ用の`CLAUDE.md追記スニペット.md`は内容反映済みのため削除。

**Flutter×Windows環境メモの反映**（2026-07-25）— ユーザー提供の`flutter-windows-env-notes.md`（Flutter SDK/Android SDKパス、AVD名、`flutter analyze`クラッシュ問題、`.bat`/git操作の既知の落とし穴等）を`docs/`に保存し、CLAUDE.mdのセッション開始チェックリストと環境構成、`docs/operation-rules.md`の完了報告前チェック・git運用から参照するよう反映。

**Information/Passage Planのサンプル表示を廃止、両方空なら再生バー非表示**（2026-07-27）— Information未入力時に出ていた台風のサンプル予報（旧`_typhoonTrackFallback`、スロット0のみ）と、Passage Plan未登録時に出ていた船のサンプル航路（旧`_shipTrackSample`）を削除。デフォルト状態では船も台風も地図に何も表示されない。これに伴い、船・台風のどちらの実データも無い場合は`_maxOffsetHours`が0になり、再生バー全体（プレイ/スピードボタン含む）を`hasTimeline`フラグで非表示にするよう`build()`を変更（従来はタイムライン帯だけが空表示になり、ボタン列は残っていた）。Ship's Name欄はUI・保存ともに残したが、ラベルとして使う先（サンプル船）が無くなったため現状未使用（`lib/screens/map_screen.dart`内にコメントで明記）。

**運用ルールにAgentレビューの既定化・非同期コード確認項目を追加**（2026-07-27）— Select CSVの非同期反映バグ（`onTap`内でawaitし忘れ）を実装前に自己レビューだけで見つけられなかったことを受け、`docs/operation-rules.md`を改訂。①項目5：複数メソッド・非同期フローにまたがる機能追加は、問題発生や3往復以上の繰り返しを待たず、実装完了時点で先にAgentレビューを行うことを明記（「順調に進んでいるから今回は自己レビューで十分」という判断自体が今回のバグと同じ「見た目の一致で済ませる」パターンだと指摘）。②項目10（新設）：非同期コードの完了報告前チェックとして、コールバックのasync化・await漏れ・fire-and-forgetの意図明記を1つずつ辿って確認することを追加。

**Passage Planの件数表示をボタンから行ごとに移動**（2026-07-27）— 「Import CSV... (n/10)」ボタンから件数表記を削除し、代わりにPassage Plan一覧の各行（Edit/Deleteアイコンの左）に「n/10」を表示する方式に変更（Edit CSVの「n/50」と統一）。Windows実機確認済み（2026-07-27、ユーザー確認OK）。

**CSVライブラリ機能の実機確認後の修正2件**（2026-07-27）— ①Select CSVで登録してもPassage Plan一覧にすぐ反映されないバグを修正（`_showSelectCsvDialog`のonTapがawaitされていなかった早すぎるリフレッシュが原因、`showDialog<String>`で選択結果を返す方式に変更）。②Edit CSVでの削除がPassage Planに反映されない点は、ユーザー確認の上で仕様変更：登録済みプランがある場合は英語で確認ダイアログ（Yes/No）を出し、Yesなら道連れ削除、無い場合は確認なしで即削除。`VoyagePlanEntry.sourceCsvFileName`を追加して紐付けを実装。詳細は`docs/devlog-csv-library.md`参照。Windows実機確認済み（2026-07-27、ユーザー確認：「実機で反映、うまくいきました」）。

**CSVライブラリ機能（Import/Select/Edit CSV）を追加**（2026-07-27）— Passage PlanのCSV取り込みを「毎回外部から選び直す」方式から、取り込んだCSVをアプリ内に蓄積（上限50件）して後から再選択できる方式に変更。Passage Planダイアログを「Import CSV／Select CSV／Edit CSV」の3段構成にし、Edit CSVでRename（新規機能）・Delete（完全削除）が可能。新規依存として`path_provider`を追加（既存依存関係経由でWindows実装は解決済みのためビルドリスク低）。設計判断・要件確定の経緯は`docs/devlog-csv-library.md`参照。**確認済み（Windows実機、ユーザーにAskUserQuestionで項目を切り分けて確認）**：①同名Import時の上書きY/N確認ダイアログ、②Select CSVで再登録できること、③Edit CSVのRename/Delete、④ライブラリの削除・改名が既存登録済みPassage Planに影響しないこと——以上4点はOK。上限50件到達時のエラー表示のみ未確認（TASKS.md参照）。Passage Plan「登録済みプラン」自体の名前（表示名）はRename対象外（ライブラリのファイル名のみ変更可）という仕様差についてもユーザー確認済み・現状維持でOK。

**InformationダイアログにTyphoon 1/2/3のClearボタンを追加**（2026-07-27）— 各TyphoonブロックのDisplayチェックボックス左側に「Clear」ボタンを配置し、押下でその枠のテキスト欄を空にする（パースエラー表示もクリア）。更新情報を貼り替える際に手動で全選択・削除する手間をなくす目的。`lib/screens/map_screen.dart`の`_showLabelSettingsDialog`内、既存の`displayCheckbox`と同じ`i`のクロージャパターンを踏襲。

**地図表示・距離計算・データ取得方式の決定**（2026-07-25）— 以前のチャットでの検討内容（中分緯度法での距離計算、簡易プロット版の地図表示、表示範囲N20-50/E115-150固定、JMA防災情報XML＋JTWCテキストのWi-Fi時取得＋オフラインキャッシュのハイブリッド方式）をCLAUDE.mdの現状要約に反映し、詳細な経緯・判断根拠は`docs/devlog-map-design.md`に退避。TASKS.mdも実際の設計に合わせて更新。

**初回git commit/push完了**（2026-07-25、`main`ブランチ、root-commit `279b179`）— `git init`実行、`user.name`/`user.email`をグローバル設定（`Capt.Edge` / `captain.edge.management@gmail.com`）した上で、`commit.bat`経由でユーザーが実行。10ファイル・396行をリモート`https://github.com/captedge/tyohoon_npo.git`（`origin`として追加済み）にpush。LF→CRLF変換の警告は無害（内容には影響なし）。

**地図モックアップレビュー・表示範囲の見直し**（2026-07-26）— 簡易プロット版モックアップを提示しフィードバックを反映。表示範囲をN20-50からN5-50/E115-150へ拡張（フィリピン全域を含めるため）、緯度経度両方のグリッド表示、ズームUI（＋／－ボタン＋スライダー追加）、UIラベル全て英語表記、再生ボタンのトグル動作を決定。経緯・教訓は`docs/devlog-map-design.md`に追記。

**第一段階のコミット/プッシュ完了**（2026-07-26、`main`ブランチ、commit `5935e4f`）— `commit.bat`経由でユーザーが実行。地図画面の初期実装一式と調査結果をリモートにpush。以後`git add .`は使わず、変更ファイル/新規フォルダを明示列挙する方式に変更済み。`windows/`配下でCRLF差分のみのノイズが出ることを確認、実害なしのため`docs/flutter-windows-env-notes.md`に記録のみ。

**投影法・表示比率の見直し（メルカトル図法化）**（2026-07-26）— 実機レビューで「日本の形が縦に縮んで見える」との指摘を受け、原因（等角経緯度図法での経度方向の未補正＋ウィンドウ形状に応じた地図の伸縮）を特定し対応。`lib/utils/map_bounds.dart`をWeb Mercator投影に変更し、表示範囲の正しい縦横比を持つ固定論理キャンバス（`MapBounds.canvasSize`）を導入。起動時は日本付近（N30/E135）を中心に表示するよう変更（`MapBounds.defaultCenterLat/Lon`）。あわせて、より高精細な海岸線データ取得の技術検証（Natural Earth 1:50m等）を行い、Coworkのファイル取得サイズ制限により今回は取得不可と判明。回避策（ユーザーが直接ダウンロードしてプロジェクトフォルダに置く）を`TASKS.md`に記録。

**海岸線データの実データ化・ズーム操作性の改善**（2026-07-26）— 仮の海岸線ポリゴンをNatural Earth 1:110m land data（TopoJSON経由）由来の実データに差し替え（`typhoon_ship_tracker/assets/coastline/coastline.json`、生成手順は同フォルダのREADME参照）。あわせてズーム処理を書き直し、マウスホイールはカーソル位置、ボタン/スライダーは画面中心を軸にズームするよう変更してパン位置リセット問題を解消。自己レビューで型エラー（`clamp`が`num`を返す問題）と`shouldRepaint`が海岸線読込完了を検知できていない不具合を発見・修正。海岸線データの詳細は`docs/devlog-map-design.md`参照。

**Windows実機初回動作確認・レビュー反映**（2026-07-26）— `flutter run -d windows`がVisual Studio導入後に成功。操作性・台風表示は好評。フィードバックを受けて航海計画の点線＋Waypointマーカー（`_drawShipRoute`）と、画面上部固定の日時表示（`dd mmm. yyyy HH:MM (JST)`形式）を追加実装。海岸線の陸海配色・船アイコンのデザインは今後相談として`TASKS.md`に記録。

**航海計画CSVの実サンプル確認**（2026-07-26）— ユーザー提供の実ファイル（`013K378NEG W-KII.csv`）で列構成を確認。公式マニュアル通りJRC ECDIS純正フォーマットで、日時列は無し。出発日時1回入力＋区間距離÷速力での到着時刻積算という設計方針を決定。詳細は`docs/data-format-notes.md`参照。

**マウスホイールズーム・縦スライダーの追加、JRC ECDIS/NAVTOR CSV調査**（2026-07-26）— ユーザーからの操作性の質問を受け、`map_screen.dart`にマウスホイールでのズームと縦方向のズームスライダーを追加（＋／－ボタンと同じ`_zoom`状態で同期）。あわせて型エラー（`clamp`が`num`を返す問題）を修正。JRC ECDIS純正ルートCSV書式を公式マニュアルで調査し、列構成（WP No/Lat/Lon/Prt/Stb/Arr Rad/Speed/Sail）を確認。ただしETA/ETD列は純正書式に含まれないため、実サンプル確認が必要と判明（詳細は`docs/data-format-notes.md`参照）。

**Flutterプロジェクト雛形＋地図画面の初期実装**（2026-07-26）— `typhoon_ship_tracker/`に`pubspec.yaml`と`lib/`（models/track_point.dart、utils/interpolation.dart・map_bounds.dart、widgets/map_painter.dart、screens/map_screen.dart、main.dart）を作成。中分緯度法での距離計算、前後2点の線形補間、グリッド＋仮海岸線の描画、InteractiveViewer＋＋／－ズームボタン、再生／一時停止トグル付き時刻スライダーを実装。海岸線は実データ未選定のため仮ポリゴン（`TODO(map-data)`）。自己レビューで`Path.extractPath`への引数型エラー（`num`→`double`）を発見・修正済み。OSプラットフォーム雛形（`windows/`等）は`flutter create`が必要でサンドボックスでは生成不可のため、ユーザー側での実行待ち。気象庁「防災情報XML」の電文種類（VPTW60）と提供形態（PULL型・無認証）を調査、詳細は`docs/data-format-notes.md`参照。

**海岸線データの1:50m化・配色の暫定変更**（2026-07-27）— 前回セッションでは取得不可と判断していたNatural Earth 1:50mデータを、`registry.npmjs.org`経由（`world-atlas@1.1.4`）で取得できることを発見し、`coastline.json`を1:110m（14ポリゴン/約300点）から1:50m（123ポリゴン/約3,000点）に差し替え。差し替え前にmatplotlibで描画して形状を目視確認済み。あわせて陸海の配色を仮の白／灰色から、ナビチャート風（淡い青の海／ベージュの陸）へ暫定変更（`map_painter.dart`の`_seaColor`等）。詳細・データ取得の教訓は`docs/devlog-map-design.md`参照。

**海岸線・配色のユーザー確認完了、グリッドラベルの追従表示化**（2026-07-27）— 上記の海岸線1:50m化・配色変更をユーザーが実機で確認し「とても良い」と確定。あわせて新規フィードバック（緯度経度グリッドラベルがズーム/パンで画面外に出る）に対応：`map_painter.dart`の`_drawGrid`からラベル描画を削除しグリッド線のみに変更、`map_screen.dart`に`_buildGridLabelOverlay`を新設し、`TransformationController`を直接listenする`AnimatedBuilder`でラベルを画面端（緯度＝左端、経度＝上端）に追従表示するオーバーレイ方式に変更。ドラッグ中もリアルタイムに追従するよう、既存の`_zoom`/`_translation`状態（interaction終了時のみ更新）ではなくコントローラー自体を直接参照する設計にした。詳細は`docs/devlog-map-overlays.md`参照。

**カーソル位置緯度経度の表示・船アイコンの向き変更**（2026-07-27）— `MapBounds`に`toOffset`の逆変換`fromOffset`（Web Mercator逆変換）を追加し往復精度をPythonで事前検証。`map_screen.dart`に`MouseRegion`を追加し、カーソル位置の緯度経度を画面右下（ズームボタン列と重ならない位置）に「31-15.5N 140-23.4E」形式（度-分.小数、秒は分の小数へ換算）で追従表示。船アイコンは、`MapPainter`に`nextWaypoint`を追加し、現在の船位置から次のWPへの方位角をキャンバス座標上で計算して三角形を回転させる方式に変更（三角形の形自体は現状維持、頂点が常に次のWPを向く）。詳細は`docs/devlog-map-overlays.md`参照。

**起動用batファイルの作成**（2026-07-27）— プロジェクトルートに`run_windows.bat`を新規作成。ダブルクリックで`typhoon_ship_tracker/`に移動し`flutter run -d windows`を実行、終了後は`pause`でウィンドウを保持。`.bat`納品前チェックリスト（CRLF変換／括弧ネストなし／日本語直書きなし）を確認済み。あわせて`commit.bat`の`git add`対象に`run_windows.bat`を追加し、`typhoon_ship_tracker`丸ごと追加から今回変更した個別ファイル列挙に変更（`windows/`配下等のCRLFノイズを巻き込まないため）。

**CLAUDE.mdの記録漏れ修正**（2026-07-27）— セッション終了前の確認で、「プロジェクト雛形の現状」（`windows/`未生成のまま止まっていた記載）と「海岸線データ」（1:110mのまま止まっていた記載）が実態と食い違っていることを発見し修正。`windows/`は2026-07-26に生成・コミット済み（commit `5935e4f`）であることをgit logで確認、海岸線は本セッションで1:50mに更新済みであることを反映。

**本セッション分のコミット/プッシュ完了**（2026-07-27、`main`ブランチ、commit `070c3d6`）— `commit.bat`経由でユーザーが実行。1回目はCoworkサンドボックス側で残った`.git/index.lock`が原因で失敗（`allow_cowork_file_delete`で削除許可を得て解消）、2回目で成功。海岸線1:50m化・配色確定・グリッドラベル/カーソル座標の追従表示・船アイコンの向き変更・`run_windows.bat`・CLAUDE.md記録修正の11ファイルをpush。LF→CRLF変換の警告は無害（過去のコミットと同様）。

**index.lock残留の原因調査・再発防止ルール策定**（2026-07-27）— 上記の`commit.bat`失敗の根本原因をユーザー依頼で追及。原因は、Coworkサンドボックスから確認目的で実行した読み取り専用の`git status`が、終了時に自分で作った`.git/index.lock`を削除しようとして権限エラーで失敗し（サンドボックスからのファイル削除失敗という既知の癖と同根）、その警告を「コミットしていないから無関係」と誤って軽視・放置したこと。Agentツール（general-purpose）による独立レビューで、対象を`index.lock`だけでなく`.git`配下の`*.lock`全般に広げるべき、警告の有無に関わらず機械的に確認すべき、という指摘を得て反映。再発防止ルールを`docs/operation-rules.md`「サンドボックスからのgitコマンド実行によるロックファイル残留」と`docs/flutter-windows-env-notes.md`（一般的な注意7）に記載。

**時刻スライダー上限のデータ駆動化・再生バーのWindy風デザイン化**（2026-07-25）— 時刻スライダーの上限を72hのハードコードから台風予報データ（`_typhoonTrack`）の最後の点から算出するデータ駆動方式に変更（JMA VPTW60の5日＝120h予報にそのまま対応、将来のJTWCデータ追加時もコード変更不要）。あわせて再生バーをWindyアプリ風のデザイン（オレンジ色の「HH:MM」バブル追従表示、最下段に「dd mmm」形式の日付セグメント行、当日ハイライト、タップでジャンプ）に刷新。

**距離表示を船の後方へ追従表示化**（2026-07-28）— 船・台風間の距離ラベルを、中間点表示→船の左側固定表示（第1段階）を経て、最終的に船の進行方向の逆側（後方）に追従表示するよう変更（濃い青灰色の塗りつぶし＋白文字の囲み付きで海・陸どちらの背景でも視認可能）。西進時に「左側固定」だと不自然になりうるとの気づきを受けての再変更。

**サンプル航海計画にWP追加**（2026-07-28）— 変針後の船アイコン向き・距離表示追従（上記）を確認できるよう、仮の航海計画データ（`_shipTrack`）を1レグ（2点）から5レグ（6点・+120hまで）に拡張。西向きのレグも含め、複数方向での見た目を確認できるようにした。

**船名・台風ラベルの追加**（2026-07-28）— 船のラベルをユーザー入力の「Ship's Name」で表示できるAppBarダイアログを追加（NAVTOR規格CSVに船名列が無いための代替入力）。あわせて`lib/utils/jtwc_parser.dart`を新規作成し、JTWC警報テキストから正規表現で番号・名称（例：「11W (NOUL)」）を抽出して台風ラベルに反映する仕組みを追加。`MapPainter`に`shipLabel`/`typhoonLabel`パラメータを追加。抽出仕様は`docs/data-format-notes.md`参照。

**複数台風対応・気圧表示・Displayトグル**（2026-07-28）— Menuダイアログを最大3台風分の貼り付け欄に拡張し、それぞれ独立にJTWC警報テキストから番号・名称・中心気圧（例：「980hPa」）・現在位置を抽出する仕組みに拡張（`jtwc_parser.dart`の`JtwcTyphoonInfo`/`parseJtwcWarningText`に統合）。台風1は従来通りサンプル予報進路で時刻追従、台風2・3は予報進路を持たない静的マーカー（`ExtraTyphoon`、`map_painter.dart`）として表示。あわせて船・台風（最大3）それぞれにDisplay ON/OFFチェックボックスを追加し、`MapPainter`の`showShip`/`showPrimaryTyphoon`/`extraTyphoons`で表示制御。抽出仕様は`docs/data-format-notes.md`参照。

**台風軌跡の常時表示化・予報点抽出**（2026-07-28）— 上記へのフィードバックを受け、台風の軌跡を船のルートと同様に過去〜未来の全区間を常時表示するよう刷新。読み込み時の最低気圧ラベルは軌跡の最初の点に固定し、再生で時刻が進んでも番号・名称ラベルのみが現在位置に追従する仕様に変更。あわせてJTWCテキストの12/24/36/48/60時間予報点を正規表現で抽出（`JtwcForecastPoint`）し、静的マーカーだった台風2・3にも実際の軌跡を持たせられるように拡張。`ExtraTyphoon`と主台風専用フィールドを`TyphoonMarker`に統合。予報点の抽出仕様は`docs/data-format-notes.md`参照。

**再生開始時刻の発表時間化**（2026-07-28）— 再生開始時刻（`_startTime`）を「現在時刻」固定から「読み込んだTyphoon 1の発表時間」（JTWC警報の`WARNING POSITION`行の`DDHHMMZ`をJSTへ変換）に変更。`jtwc_parser.dart`に`issuedAtJst`を追加し、`map_screen.dart`の`_startTime`を可変化、依存する`_shipTrack`/`_typhoonTrackFallback`をゲッター化して再計算されるようにした。発表時刻の解決仕様は`docs/data-format-notes.md`参照。

**再生スピード調整**（2026-07-28）— 再生スピードを25%〜150%（デフォルト50%）でAppBarから調整できるダイアログを追加。

**clean_project.batの作成**（2026-07-25）— Androidアプリ開発での既存の習慣（ビルド肥大化対策）を踏襲し、プロジェクトフォルダ直下に`clean_project.bat`を新設。`typhoon_ship_tracker/`で`flutter clean`を実行し`build/`・`.dart_tool/`を削除する。作成時点で実測`build/`228MB・`.dart_tool/`42MBだったが、ユーザーが実行後に両フォルダとも削除されていることを確認済み。実行タイミングはユーザーの任意（習慣化済みのため定期実行の仕組み化は不要と判断）。

**マウスホイールズームの不具合修正**（2026-07-28、3往復）— マウスホイールでのズームアウトが最小まで届かない不具合を調査。1回目（`PointerSignalResolver`経由での登録）は効果なし、2回目で独自のホイール処理を撤去し`InteractiveViewer`本来の処理に一本化（副作用対応として`_transformationController`への永続リスナーを追加）、3回目でズーム最小値を「cover」フィット基準（新設`_coverFitScale`）に変更しホイール・ボタン・スライダーの限界を統一。経緯・原因・教訓は`docs/devlog-wheel-zoom.md`に集約。

**表示エリアの東西範囲拡張（東経70°〜180°、後に85°〜170°へ微調整）**（2026-07-25）— TASKS.mdで決定待ちだった東西表示範囲拡張が決定し対応。`MapBounds`（`lib/utils/map_bounds.dart`）の`minLon`/`maxLon`を115/150から70/180に変更（緯度N5-50は変更なし）。海岸線データも同じNatural Earth 1:50mソース・手法（`registry.npmjs.org`経由でworld-atlas取得→TopoJSON手動デコード→shapelyでunion・クリップ・簡略化）でE70-180に再クリップ。その後ユーザーの好み（技術的理由ではなく見た目の好み）で東経85°〜170°に絞り込み確定（緯度は変更なし）。最終的な海岸線データは198ポリゴン・約4,510点・~73KB。クリップ後にmatplotlibで形状を目視確認済み。グリッド線・グリッドラベルオーバーレイは`MapBounds.minLon/maxLon`を動的参照する設計だったため、コード変更不要で表示範囲変更に追従することを確認済み。詳細は`typhoon_ship_tracker/assets/coastline/README.md`参照。

**航海計画CSVインポート・出発日時入力・WP追加/区間速力編集の実装**（2026-07-30）— `docs/data-format-notes.md`で確定済みだったJRC ECDIS/NAVTORルートCSV形式（17列、`//`コメント行、WP000の`***`列）のパーサーを実装。ユーザー提供の実サンプル`NEG AW-KII.csv`（24 WP、コメント3行含む28行）で列数・緯度経度換算・区間距離÷速力の時刻積算ロジックをPythonで再現検証済み（総距離約599.6NM、WP0発9:00→WP23着9:52、約48.9時間、速力欠落なし）。実装内容：`lib/models/ship_waypoint.dart`（`ShipWaypoint`、時刻を持たず区間速力`speedKn`のみ保持）、`lib/utils/voyage_plan_parser.dart`（CSV→`ShipWaypoint`リスト、列数不足や東西南北不正値は`VoyagePlanParseException`）、`lib/utils/voyage_plan.dart`（`shipTrackFromWaypoints`：既存の中分緯度法`distanceNm`で区間距離を算出し区間速力で除算、`TrackPoint`リストへ変換。速力未設定は`VoyagePlanTimeException`）、`lib/screens/voyage_plan_screen.dart`（WP一覧の表形式編集画面：緯度経度・速力・名称の直接編集、行の追加/削除、出発日時ピッカー、Save時に変換ロジックで検証してから確定）。`map_screen.dart`のAppBarに船アイコンのメニュー（Import CSV.../Edit voyage plan...）を追加し、`file_picker`パッケージ（pubspec.yamlに追加、`^8.1.2`）でファイル選択→パース→編集画面→保存の流れを実装。既存のサンプル航海計画（`_shipTrackSample`）はCSV未取込時のフォールバックとして維持。緯度経度は現版では10進度で編集（他画面の度分表記と揃えるかは要確認、TASKS.md参照）。CoworkサンドボックスがLinuxのためビルド未実施——ユーザーのWindows機での`flutter pub get`＋`flutter run -d windows`での動作確認が必要。

**再生バーの日付ずれバグ修正**（2026-07-30）— 台風情報（JTWC警報テキスト）読込時、再生開始時刻の「時刻」（HH:MM）は正しいのに再生バー下段の日付区切り（例：25 Jul/26 Jul）が1日ずれる不具合をユーザー報告により修正。原因は`jtwc_parser.dart`の`issuedAtJst()`が`DateTime.utc(...).add(Duration(hours:9))`でUTCタグ付きの日時オブジェクトを生成していたこと。アプリの他の箇所（`map_screen.dart`の`_dayColumns`が使う`DateTime(_startTime.year, _startTime.month, _startTime.day)`等）はすべてタグなしの素の日時として扱う設計のため、実行環境（PC）のタイムゾーン設定によっては同じ数値でも「実際の瞬間」が数時間ずれ、日付境界の判定がおかしくなっていた。修正は`DateTime.utc`を使わず、UTC→JST（+9h）の日またぎを手計算（`day + dayCarry`）して素のDateTimeを組み立てる方式に変更。Pythonでの再現計算（250600Z→25日15:00、250000Z→25日9:00、252200Z→26日7:00）で検証済み。

**軌跡の実線/点線分割・オブジェクトのズーム固定サイズ化**（2026-08-03）— 2点の見た目改善要望に対応。①船・台風とも、現在時刻より前（通過済み）の軌跡を実線、後（予定）を点線で描き分け。`lib/utils/interpolation.dart`に`splitTrackAtTime`を新設（現在時刻の補間点を境界として past/future の2リストに分割、境界点を両方に含めて線がつながるようにする）。Python側で境界点の連続性を検証済み。②船アイコン・台風アイコン・各ラベル（船名、台風番号、気圧、距離nm）・航路上の小さな丸マーカーが、地図をズームすると画面上でも大きく/小さくなってしまう問題を修正。`MapPainter`に`zoom`パラメータを追加し、各マーカー/ラベル描画箇所を`canvas.translate`+`canvas.scale(1/zoom)`で囲むことで、地図のズーム倍率を打ち消し画面上のサイズを常に一定に保つようにした（航路・軌跡の線自体は従来通り地図と一緒にズームする仕様を維持）。`map_screen.dart`から`MapPainter`へ`zoom: _zoom`・`shipPastRoute`/`shipFutureRoute`・台風スロットごとの`pastTrack`/`futureTrack`を渡すよう連携。

**実線/点線の太さもズーム固定化・船と台風で太さ統一**（2026-08-03）— 前回の軌跡実線/点線分割・オブジェクトのズーム固定サイズ化に続き、線の太さもズームで変わらないよう修正。`MapPainter._drawPolyline`のPaintの`strokeWidth`を「画面上で見せたい太さ×(1/zoom)」で設定する方式に変更（マーカー/ラベルと同じ「1/zoomを打ち消す」考え方だが、線は始点終点がシーン座標のままズーム/パンに追従する必要があるため、canvas.scaleではなくstrokeWidth自体をスケール）。あわせて船ルート（従来1.5）と台風軌跡（従来2）で別々だった太さを`_trackStrokeWidthPx = 2.0`に統一。

**点線パターン（長さ・間隔）の統一・細かく調整**（2026-08-03）— 船ルート（従来: 長さ4/間隔3）と台風軌跡（従来: 長さ6/間隔4、デフォルト値のまま個別調整されていなかった）で異なっていた点線パターンを、`_trackDashLengthPx=3.0`/`_trackGapLengthPx=2.0`（従来の船より少し細かい）に統一。`MapPainter._drawPolyline`のデフォルト引数化により両者が自動的に同じ値を使うようにした。

**再生バーの終点を航海計画の最終WPに変更**（2026-08-10）— `map_screen.dart`の`_maxOffsetHours`（再生バー・スライダーの上限時間）を、台風スロット0の最終予報点基準から船の航海計画の最終WP到着時刻基準に変更。台風データの予報期間が船の航海より短くても、再生バーは船が到着地に着くまで進めるようにした（台風マーカーは自身の軌跡の最終点を過ぎると、`positionAt`のクランプ挙動によりそこで静止表示される）。サンプルデータ（未インポート時）は船・台風とも従来通り+120hで揃っているため見た目上の変化はない。

**船名・台風名ラベルの後方表示化**（2026-08-14）— 船名・台風番号（designation）ラベルが常にアイコンの右横固定だった見た目を、進行方向の後方（distance表示と同じ「behind」方向）に常時追従するよう変更。船側は既存の距離表示（distance box）と重ならないよう、船名を内側・距離を外側にスタック配置（`_drawShip`内で共通の`behind`方向を使い、`_drawDistanceLabel`を独自translate不要な形に書き換えて呼び出す方式に統合）。台風側は`futureTrack`の直後の点（無ければ`pastTrack`の直近区間）から進行方向を推定する`_typhoonBehindDirection`を新設。気圧ラベル（track.first固定）は対象外・変更なし。文字サイズ・色・太さは既存のまま。`typhoon_ship_tracker/lib/widgets/map_painter.dart`のみ変更。確認済み：コメント除く括弧・波括弧の対応が0で一致（構文上の破綻なし）。Coworkサンドボックス（Linux）のためビルド未実施——Windows機での`flutter run -d windows`での見た目確認が必要。

**gitサブコマンドのホワイトリスト化（誤操作対応）**（2026-08-14）— 上記の確認作業中、サンドボックスから禁止対象の`git stash`を誤って実行（直前の`git diff`が残した`index.lock`により偶然失敗、実害なし）。ユーザーから「失敗＝実害なしは結果論」「『つもり』は機械的基準でない」と指摘を受け、`docs/operation-rules.md`のgit運用ルールを「add/commit/pushを禁止」という書き方から「サンドボックスで実行してよいのは`status`/`diff`/`log`のみ」というホワイトリスト方式に強化。詳細・教訓は同ファイルの「サンドボックスから実行してよいgitサブコマンドのホワイトリスト」参照。

**台風の100nm/200nm距離同心円の追加**（2026-08-14）— 台風中心（赤丸）から100nm・200nmの同心円を表示できる機能を追加。`TyphoonMarker`に`showRings`フラグを追加し、`MapPainter`に円（塗りなし・アウトラインのみ、100nm=ティール／200nm=パープル、いったんお任せ配色）とラベル（円の外側・真上、フォントサイズ9・ズームで固定サイズ）の描画を実装。円自体は地理的な距離を表すため地図と一緒にズーム/パンする一方、線の太さとラベルは他の要素と同じ`1/zoom`補正で画面上一定サイズを維持。ラベル位置は前回の同心円追加確認時のユーザー指示「情報→上方」を反映し円の真上（12時位置）に統一。距離→キャンバスpx換算は`MapBounds`のWeb Mercatorが等角図法である性質を利用し、緯度に応じた単一のスケール係数`_pxPerNm(lat)`で算出（PythonでのMercator微分検証は行わず、コード内コメントで導出過程を明記）。切り替えはAppBarに追加した`100/200nm rings`メニュー（`CheckedPopupMenuItem`でワンクリックON/OFF）と、地図上の台風アイコン（赤丸）タップの両方から可能（`_handleMapTap`、地図キャンバスを`GestureDetector(behavior: opaque)`で包み、タップ位置とアイコン位置の距離を`1/zoom`補正した許容半径で判定）。`_TyphoonSlot.ringsEnabled`（既定false）で状態管理。確認済み：`map_screen.dart`・`map_painter.dart`ともコメント除く括弧・波括弧の対応が0で一致。Coworkサンドボックス（Linux）のためビルド未実施——Windows機での実機確認（同心円の見た目・タップ判定・メニュー切り替え）が必要。

**JST日時表示を右下（カーソル緯度経度の上）へ移動**（2026-08-16）— 左上固定だった日時表示が、拡大率によっては左上の緯度経度グリッドラベルと重なる不具合をユーザー報告により修正。`map_screen.dart`のPositioned（left:12,top:12）を削除し、右下のカーソル緯度経度表示（right:64,bottom:12）と同じColumnにまとめて、日時を上・カーソル緯度経度を下に縦積み表示するよう変更（カーソル非表示時は日時のみ表示）。確認済み：コメント除く括弧・波括弧の対応が0で一致。Windows機での実機確認が必要。

**船・台風アイコンを画像化**（2026-07-27）— プレースホルダーの三角形（船）・赤丸（台風）を、`assets/ship_icon01.png`／`assets/typhoon_icon01.png`の実画像に差し替え。台風アイコンは画像中心を座標に、船アイコンは左右中央・下から15%の位置を座標に（`_shipIconAnchorYFrac=0.85`）アンカーする方式で実装。船アイコンは既存の進行方向（次WP）回転ロジックをそのまま流用（画像の船首が上向きの絵のため、従来の三角形と同じ「angle=0で上向き」の前提と一致）。表示サイズはズームに対して固定（既存の`1/zoom`補正パターンを踏襲、船アイコン高さ26px・台風アイコン直径24px、従来のプレースホルダーに近いサイズ）。画像は`dart:ui`の`instantiateImageCodec`で`map_screen.dart`の`initState`で1回だけ読み込み（`lib/utils/marker_icons.dart`新設）、読み込み完了までは従来のプレースホルダー形状にフォールバックする設計。`pubspec.yaml`のassetsに両PNGを追加。確認済み：`map_painter.dart`・`map_screen.dart`・`marker_icons.dart`ともコメント除く括弧・波括弧・角括弧の対応が0で一致。Windows機での実機確認済み（2026-07-27、ユーザー確認：アイコンの向き・アンカー位置・ズーム時のサイズ固定いずれもOK）。

**Playback speedメニューを再生バー内へ移動**（2026-07-27）— AppBar上段にあった速度調整アイコン（`Icons.speed`）を撤去し、左下の再生バー内、再生／一時停止ボタンの真上に縦積みで配置。列の幅（52px）・再生バー自体の幅は変更せず、両ボタンとも`padding: EdgeInsets.zero`＋`constraints`縮小・アイコンサイズ縮小（速度18px、再生28px、従来34px）で既存の88px高の枠内に収めた。確認済み：`map_screen.dart`の括弧・波括弧・角括弧の対応が0で一致。Windows機での実機確認済み（2026-07-27、ユーザー確認：見た目・タップしやすさともOK）。

**Playback speedの調整範囲を1%〜100%に変更**（2026-07-27）— 従来25%〜150%だったスライダー範囲を、ユーザー要望により1%〜100%へ変更（`_minPlaybackSpeed`/`_maxPlaybackSpeed`）。刻み幅も新しい整数%範囲に合わせ5%刻みから1%刻みに変更。デフォルト値（50%）は新範囲内のためそのまま維持。確認済み：括弧・波括弧・角括弧の対応が0で一致。Windows機での実機確認済み（2026-07-27、ユーザー確認OK）。

**メニューポップアップの表示名変更**（2026-07-27）— ①「Playback speed」→「Play Sp'd」（ダイアログタイトル・再生バー内アイコンのtooltip両方）、②台風の距離同心円メニューのtooltip「100/200nm rings」→「Range Ring」、③「Voyage plan」→「Passage Plan」（AppBarメニューのtooltip、`VoyagePlanScreen`のタイトル、メニュー項目「Edit voyage plan...」→「Edit passage plan...」、CSVファイル選択ダイアログのタイトルまで統一）。コード内部の変数・クラス名（`VoyagePlanScreen`等）はユーザー非表示のため変更対象外。確認済み：`map_screen.dart`・`voyage_plan_screen.dart`とも括弧・波括弧・角括弧の対応が0で一致。Windows機での実機確認済み（2026-07-27、ユーザー確認OK）。

**船・台風の名前メニューの表示名変更**（2026-07-27）— 「Ship's Name / Typhoon label」（tooltip）・「Ship's Name / Typhoons」（ダイアログタイトル）を「Information」に変更（当初「Infromation」と指定されたがスペルミスと判明し即修正）。ダイアログ内の各入力欄の見出し（"Ship's Name"、"Typhoon 1/2/3"）はメニュー名自体ではないため変更対象外。確認済み：括弧・波括弧・角括弧の対応が0で一致。Windows機での実機確認済み（2026-07-27、ユーザー確認OK）。

**Passage Planの複数CSV登録・個別Edit/Delete/Display・ルート独立比較表示**（2026-08-xx）— 単一プランのみだった航海計画を、最大10件のCSVを登録・個別にEdit/Delete/Display ON-OFFできる仕様に拡張。当初は「Display ONの複数プランを出発時刻順に連結して1つの航跡にする」（寄港・再出発の想定）で実装したが、ユーザーから訂正が入り「同一出発港・同時刻から行き先が違う複数ルートを比較したいだけで、連結は不要。チェックしたものはそれぞれ独立に動かしたい」という要件に修正。`lib/models/voyage_plan_entry.dart`新設（`VoyagePlanEntry`）、`lib/widgets/map_painter.dart`を単一船描画から`ShipMarker`（`TyphoonMarker`と同じ複数独立描画パターン）に全面書き直し、各ルートが自分の位置・past/future分割・次WP方位・台風までの距離を独立に持つ設計に変更。AppBarの船アイコンから開く単一ダイアログ（Import CSV.../各行Display・Edit・Delete）を追加。あわせて、Agentレビューで指摘された2点をユーザーに確認して反映：①Informationメニューの「Ship's Name」のDisplayチェックボックス（プラン登録後は無視される死んだUIだった）を削除しテキスト欄のみに、②同一出発点での複数ルート比較時に見分けがつくよう`ShipMarker.color`（10色パレット）を追加しルート線・waypointドット・船アイコン（`ColorFilter`によるシルエット化）に反映。Agentによる独立レビューを複数回実施（構文・型・null安全性・既存ロジックとの整合性を確認、最終的に指摘なし）。経緯・訂正の教訓は`docs/devlog-passage-plan-multi.md`参照。Coworkサンドボックス（Linux）のためビルド未実施——Windows機での実機確認が必要（特に、複数ルート比較時の色分け・アイコン視認性、Ship's Name欄の見た目）。

**再生バーが完全に消える不具合の防止策を追加**（2026-07-27）— 月またぎ修正のテスト中にユーザーが遭遇した「再生バーが表示されない」現象を調査。原因は、以前のセッションで登録済み（永続化復元）のPassage Planの出発/到着時刻が、テスト用に貼り付けた台風テキストの発表時刻（PC日付を進めて解決した`_startTime`）より前になってしまい、`_maxOffsetHours`（`map_screen.dart`）が船側のいずれの経路も「未来方向の時間」を持たないと判定して0を返し、`_buildTimelineTrack`が再生バー自体を非表示にしていたこと。日付時刻の計算ロジック自体（今回の月またぎ修正）は正しく動作していた（右下の日時表示が意図通り8/1 0300だったことからも確認）。対応として、`_maxOffsetHours`に安全策を追加：表示中のいずれかの船の到着が実際に未来（`_startTime`より後）にある通常ケースでは従来通り船到着時刻基準（2026-08-10の決定を変更なし）。表示中の船が全て既に過去（到着済み）の場合のみ、表示中の台風の最終予報点までを上限とするフォールバックに切り替え、再生バーが完全に消えることを防止。確認済み：`map_screen.dart`のコメント除く丸括弧・波括弧・角括弧の対応が0で一致。Windows機での実機確認済み（2026-07-27、ユーザー確認OK：同じテスト条件で再生バーが表示されること、通常ケース＝Passage Planの到着が未来の場合の挙動に変化がないこと、両方確認）。

**JTWC発表時刻の月またぎ誤判定を修正**（2026-07-27）— ユーザーからの質問「JTWC発表が\"311800Z\"（31日）で、PC入力時刻が\"8/1 03:30\"（月をまたいだ後）の場合、`issuedAtJst`は月をどう判断するか」を受けて検証したところ、実際に誤動作するケースだと判明し修正。旧ロジックは`DateTime(reference.year, reference.month, day + dayCarry, ...)`という「参照日時の月をそのまま使い、日付超過はDateTimeの自動繰り上げに任せる」方式だったため、参照日時（PCの現在時刻）が既に翌月に入っている場合、7月31日+9h=8月1日となるべきところが「8月32日」→自動繰り上げで「9月1日」に誤って1か月分余分に進んでしまう不具合があった（reference.monthが「元の日付が属する月」ではなく「繰り上げ後の月」を指してしまっているケース）。修正は、reference.monthを使う候補と、その前月を使う候補の2つを計算し、reference+6時間以内（未来方向の許容誤差）に収まる候補のうち最も新しい方を採用する方式に変更（`lib/utils/jtwc_parser.dart`の`issuedAtJst`）。年またぎ（12月→1月）も同じロジックでカバー（前月計算時に年を繰り下げ）。Pythonで6パターン（今回の報告ケース、通常の同月ケース、月末深夜に入力した既存の正常ケース、dayCarryなしでの月またぎ、年またぎ、既存doc記載の250000Zケース）を再現検証し、いずれも期待通りの日時になることを確認済み（回帰なし）。確認済み：括弧の対応チェックは正規表現内の記号に起因する固定の差分のみ（内容は無変化）。Windows機での実機確認済み（2026-07-27、ユーザー確認OK：PC日時を進めた状態で月またぎのサンプルを貼り付け、日時表示が正しく計算されることを確認。なお同時に再生バーが消える別件不具合を発見——上記「再生バーが完全に消える不具合の防止策を追加」参照）。

**台風の予想地点に時刻ラベル（dd/HH, JST）を追加**（2026-07-27）— 台風の軌跡上の各点（読込時の現在位置＋各予報点）の円の右側に、その地点の有効時刻をJSTの「dd/HH」形式（例：270600Z→27/15）で表示する機能を追加。`TrackPoint.time`は`_startTime`（`issuedAtJst`でUTC→JST変換済みの発表時刻）からの経過時間として組み立てられており、既に素のJST時刻として扱う設計（`day`/`hour`をそのまま読むだけで正しいJST値になる、DateTime.utcを使わない既存規約通り）だったため、追加の時刻変換ロジックは不要だった。`MapPainter.TyphoonMarker`に`trackTimes`（`track`と同じ順序の`DateTime`リスト）を追加し、`map_screen.dart`の`_typhoonMarkers`から`points.map((p) => p.time)`で連携。ラベルはRange Ring同様のズーム固定サイズ・フォントサイズ9（「同じ大きさで」の要望に合わせ、色のみ黒に変更）。ユーザー提供の実サンプル（12W DOLPHIN、270600Z→27/15、271800Z→28/03等）でPythonにより計算を再現検証済み。確認済み：`map_painter.dart`・`map_screen.dart`ともコメント除く丸括弧・波括弧・角括弧の対応が0で一致。Windows機での実機確認済み（2026-07-27、ユーザー確認OK）。

**JTWC警報テキストの認識対象を「TYPHOON」以外の熱帯低気圧段階にも拡大**（2026-07-27）— 最新のJTWC警報テキスト（「TROPICAL STORM 12W (DOLPHIN)」）を貼り付けるとエラーになる不具合をユーザー報告により修正。原因は`jtwc_parser.dart`の番号/名称抽出パターンが文字列`TYPHOON`のみに一致し、`TROPICAL STORM`/`TROPICAL DEPRESSION`等の段階では一致しなかったこと。ユーザーの意向（「台風として認識して構わない」）を受け、`SUPER TYPHOON`/`TYPHOON`/`SEVERE TROPICAL STORM`/`TROPICAL STORM`/`TROPICAL DEPRESSION`のいずれにも一致するよう正規表現を拡張（分類の種類自体はラベルとして保持せず、番号/名称のみ抽出する設計は維持）。ユーザー提供の実サンプル（12W DOLPHIN、TROPICAL STORM段階）をPythonで正規表現の再現検証し、番号/名称・気圧・現在位置・警報時刻・12/24h予報点すべてが正しく抽出されることを確認済み。確認済み：括弧の対応チェックは正規表現内の記号（`[^)]`等）に起因する固定の見かけ上の差分（修正前後で同じ差分値、内容は無変化）のみで、実際の構文崩れなし。Windows機での実機確認済み（2026-07-27、ユーザー確認OK：同テキストの再貼り付けでエラーが解消し表示された）。

**ポータブルZip配布用のbuild_release.batを作成**（2026-07-27）— ユーザーからの「run_windows以外に、実際にインストールして試せる配布形式を作れるか」という質問に対し、Windowsインストーラー（Inno Setup等）とポータブルZipの2案を提示、後者（追加ツール不要・簡単）を選択いただいた。プロジェクトルートに`build_release.bat`を新設：`flutter build windows --release`でリリースビルドを実行後、`build\windows\x64\runner\Release\`フォルダ一式（実行ファイル・DLL・assetsを含む自己完結フォルダ）をPowerShellの`Compress-Archive`で`TyphoonShipTracker.zip`に圧縮する2段階構成。Coworkサンドボックス（Linux）のためWindows実行ファイルのビルド自体はできず、ユーザーのWindows機での実行が必要。確認済み：CRLF変換済み・括弧ブロックなし（`if errorlevel 1 goto`形式のみ使用）・日本語直書きなし。Windows機での実機確認済み（2026-07-27、ユーザー確認OK：インストール完了）。

**登録情報の永続化（次回起動時の自動読込）**（2026-07-27）— ユーザーからの確認依頼「アプリを閉じ、また開いた場合に直前の入力済の登録情報が読み込まれる仕様になっているか」に対し、実装済みではなかったことを確認した上で追加実装。`shared_preferences`（`^2.3.2`）を`pubspec.yaml`に追加（Windows実装はプレーンなDart file I/OでネイティブビルドやCMake変更不要、symlinkサポートも`file_picker`追加時に有効化済みのため追加要件なし）。`lib/utils/app_state_storage.dart`を新設し、船名・Passage Plan（複数CSVエントリの名前/WP一覧/出発日時/Display状態）・台風スロット（貼り付けたJTWC生テキスト・Display・Range Ring状態）・再生速度を1つのJSON blobとしてシリアライズ。台風情報は解析済みデータではなく生テキストを保存し、起動時に既存の`parseJtwcWarningText`で再解析する設計（パーサーの将来的な改善が自動的に反映されるため）。`map_screen.dart`の`initState`で起動時に非同期読込（`_restoreState`）、Passage Plan追加/編集/削除/Display切替、台風情報保存、Range Ring切替（メニュー・地図タップ両方）、再生速度保存の各箇所に`_saveState()`（fire-and-forget）を追加。地図のズーム/位置・再生スライダーの現在位置は対象外（毎回リセット）。確認済み：`map_screen.dart`・`app_state_storage.dart`ともコメント除く丸括弧・波括弧・角括弧の対応が0で一致。Windows機での実機確認済み（2026-07-27、ユーザー確認OK：`flutter pub get`＋起動後、前回入力内容が復元されることを確認）。

**本セッション分のコミット/プッシュ完了**（2026-07-28、`main`ブランチ、commit `8d7713c`）— `commit.bat`経由でユーザーが実行。Passage Plan複数CSV対応・CSVライブラリ機能・Information/Passage Planサンプル表示廃止・今後の開発方針決定（`docs/devlog-architecture-roadmap.md`新規）をpush。事前にサンドボックスの`git status`実行で`.git/index.lock`の残留を検知し、`allow_cowork_file_delete`で削除・再確認してから`commit.bat`実行を案内（再発防止ルール通りの対応）。あわせて配布用ビルド生成物（`TyphoonShipTracker/`、`TyphoonShipTracker.zip`）を`.gitignore`に追加し以後のgit管理対象から除外。

**今後の開発方針（オンライン化・スマホ対応）のプロジェクト構成を決定**（2026-07-28）— オフライン版完成を受け、次フェーズ（①JMA XML自動取得・Wi-Fiキャッシュ、②Android/iOS対応）の進め方を相談。同一プロジェクト・同一`main`ブランチを継続し、フィーチャーブランチを都度マージする通常運用とすることを決定（複数の完成形を並行維持する運用は取らない）。オンライン化はアプリ内ON/OFF設定として実装し別バージョンにはしない方針、スマホ版UIは機能・デザインをデスクトップ版と極力共通化し横向き固定とする方針。進める順序は①→②。あわせてユーザー参照用に方針をまとめたWord資料を作成。判断の経緯・教訓は`docs/devlog-architecture-roadmap.md`参照（仮方針・変更可能性あり）。

**JMA防災情報XML(VPTW60)のスキーマ調査完了**（2026-07-28）— `extra.xml`一覧フィード（随時発表、`regular.xml`ではない）から実際のVPTW60電文URLを取得し実データで構造を確認。電文URLは発表毎に変化するため一覧フィードを都度取り直す必要があること、座標が`+緯度+経度/`という符号付き独自フォーマットであること、`ReportDateTime`がJST付きISO8601で年月またぎ推定が不要なこと（JTWCと異なる点）、台風発生前は名称・番号が空文字になりうることを確認。詳細スキーマは`docs/data-format-notes.md`に記録。次はDartパーサー実装（TASKS.md参照）。

**JMA防災情報XML(VPTW60)のDartパーサー実装**（2026-07-28）— `lib/utils/jma_xml_parser.dart`を新規作成し、`parseJmaTyphoonXml(String)`で電文XML文字列を`JmaTyphoonInfo`（実況位置・気圧・分類・予報トラック）に変換する実装を追加。JTWCパーサーと異なり電文が年月日付きJST時刻を持つため月またぎ推定ロジックは不要（`+09:00`以外のオフセットは例外で検出）、`+緯度+経度/`という独自座標フォーマットは正規表現でパース。`JmaTyphoonInfo.toTrackPoints()`で既存の`TrackPoint`リストへ直接変換できる設計とし、`MapScreen`統合（次タスク）に備えた。純Dartの`xml`パッケージ（`^7.0.1`、ネイティブビルド要件なし）を新規依存として追加。確認済み：①括弧の対応が0で一致（全角/半角の混在ミスを1件発見・修正）。②Pythonで同じ抽出ロジックを再現し、実際にJMAから取得したサンプル電文（`20260716190017_0_VPTW60_010000.xml`）に対して実況位置(5.6N,153.8E)・気圧1006hPa・分類・予報3点（12/24/48h）の値がXML本文と一致することを検証済み。③Agentツール（general-purpose）による独立レビューを実施（`package:xml`の実APIをpub.dev公式ドキュメントで裏取りした上でのAPI使用の正しさ、null安全性、設計意図と実装の一致、既存パーサーとの命名・コメント一貫性を確認）——重大な指摘なし、軽微な指摘2件（ルート要素なしの空XMLでの`StateError`未捕捉、タイムゾーンオフセット欠落時の扱いのdoc comment上の曖昧さ）は同セッション内で反映済み。Coworkサンドボックス（Linux、Dart SDKなし）のため`flutter pub get`・実際のDartコンパイルは未実施——Windows機での`flutter pub get`＋ビルド確認が必要。詳細は`docs/data-format-notes.md`参照。

**JMA自動取得の地図画面への接続（手動トリガー版）**（2026-07-28）— `lib/utils/jma_feed_fetcher.dart`を新規作成し、`extra.xml`一覧フィード取得→最新VPTW60電文URL解決→電文取得→パースを`fetchLatestJmaTyphoon()`として実装（`dart:io HttpClient`使用、新規パッケージ依存なし）。`MapScreen`の「Information」ダイアログに台風スロットごとの「Fetch from JMA」ボタンを追加し、押すと取得結果を既存の`JtwcTyphoonInfo`型に変換してSave経路（テキスト貼り付けと共通）に流し込む設計。スロット0での取得はJMA電文自身の厳密なJST時刻を再生開始時刻に直接反映（JTWC側の月またぎ推定は不要）。Agentツールによる独立レビューを実施し、重大な指摘1件（ダイアログの`barrierDismissible`が既定trueのため、フェッチ中に画面外タップで閉じるとCancel/Saveを経由せず`dialogOpen`フラグが更新されず、フェッチ完了時に破棄済みダイアログへ`setState`してクラッシュしうる）を`barrierDismissible: false`指定で修正。確認済み：①括弧の対応が0で一致。②Agentレビューで軽微な指摘（`hoursFromNow`の整数変換が端数を切り捨てる設計上の非可逆性等）は許容範囲と判断し対応見送り（コメントで明記）。現状は手動トリガー（ボタン押下時のみ取得）で、自動定期取得・オフラインキャッシュ・複数台風同時発表時の区別は未実装（TASKS.md参照）。取得結果はセッション内メモリのみで次回起動時には残らない。Coworkサンドボックス（Linux、Dart SDKなし）のため`flutter pub get`は前回セッションでWindows実機確認済みだが、今回の新規コードのビルド・実機動作確認は未実施——Windows機での確認が必要。詳細は`docs/data-format-notes.md`参照。

**JTWC/JMA同時表示・色分け・距離のOn-Off連動**（2026-07-28）— ユーザー要望「気象庁と米軍の予報を両方表示、それぞれDisplayで単独表示可能に。色分けし（米軍は赤）、船との距離もそのOn/Offに連動し同じ色で」に対応。`_TyphoonSlot`の単一`info`/`displayEnabled`を`jtwcInfo`/`jtwcDisplayEnabled`（JTWC）と`jmaInfo`（`JmaTyphoonInfo`型、`jma_xml_parser.dart`の`toTrackPoints()`で絶対時刻のまま扱う——前回セッションでJTWC形式へ変換していた際にAgentレビューで指摘された「相対時間変換の非可逆性」を踏まえた設計変更）/`jmaDisplayEnabled`（JMA）に分離し、両系統を独立にDisplay On/Off・同時表示できるようにした。`TyphoonMarker`に`color`フィールドを追加（JTWC=赤・JMA=オレンジ、軌跡線・マーカー・アイコン色フィルタ・ラベルすべてに反映）。`ShipMarker.distanceToTyphoonNm`（単一値）を`ShipMarker.typhoonDistances`（`List<ShipTyphoonDistance>`）に置き換え、表示中の台風マーカーそれぞれへの距離を、その台風と同じ色で船の後方にスタック表示するよう変更（`map_painter.dart`）。Fetch from JMA成功時は自動的にJMA側のDisplayをONにする挙動を追加。JMA自動取得は引き続きセッション限り（永続化対象外）。Agentツールによる独立レビューを実施し、重大な指摘なし（軽微なdocコメントの古さ1件を反映済み）。Coworkサンドボックス（Linux、Dart SDKなし）のためビルド・実機動作確認は未実施——Windows機での確認が必要（特に色分けの見やすさ、複数距離ボックスのスタック表示、Typhoon 2/3での同様の動作）。

**台風ラベルの表記変更（ソース名＋号数/発達段階コード）・距離ボックスの配色変更**（2026-07-28）— 実機確認後のフィードバック2件に対応。①ラベル表記：JTWCは「JTWC」＋既存の号数表記（例「JTWC12W (NOUL)」）、JMAは「JMA」＋号数＋気象庁公式の発達段階コード（例「JMA13TS (DOLPHIN)」、熱帯低気圧予想段階など号数未確定時はコードのみ「JMATD」）とする方針をAskUserQuestionでユーザーに確認（「Tの意図」「TD段階の表示」の2問）。ユーザー回答は「気象庁の気象図に表示される公式符号をそのまま使う」「TD等の発達段階コードをそのまま表示（推奨案）」で確定。`jma_xml_parser.dart`の`JmaTyphoonInfo`に`classCode`ゲッター（`classification`文字列末尾の括弧内英字コードを正規表現抽出、例「熱帯低気圧(TD)」→「TD」）を新設し、`designation`ゲッターを従来の日本語「台風13号」形式から「13TS」のような号数＋コード形式に変更（号数未確定でもコードのみで非nullを返すよう仕様変更）。Pythonで実データの分類文字列（熱帯低気圧(TD)／台風(TS)／強い台風(STS)）に対する正規表現抽出を検証済み。`map_screen.dart`の`_typhoonMarkers`でJTWC/JMA双方のマーカーラベルにソース名を前置し、Range Ringメニューの表示名も統一する`_slotMenuLabel`を新設。②距離ボックス：枠色のみで台風を色分けしていたが判別しにくいとの指摘を受け、塗りつぶしを対応色（不透明度0.92）・枠を黒に変更（`map_painter.dart`の`_drawDistanceLabel`）。確認済み：3ファイルとも括弧の対応が0で一致。Coworkサンドボックス（Linux、Dart SDKなし）のためビルド未実施——Windows機での見た目確認が必要。

**Range Ringのソース別（JTWC/JMA）独立On/Off化**（2026-07-28）— 「Range Ringは双方表示している際に別々にOn/Offができるように。メニューバーも台風ラベルで選べるように」との要望に対応。`_TyphoonSlot.ringsEnabled`（スロット共有の単一フラグ）を`jtwcRingsEnabled`/`jmaRingsEnabled`に分離し、台風アイコンタップ（`_handleMapTap`）・AppBarのRange Ringメニューとも、タップ/選択した方のソースだけを独立して切り替えるよう変更。メニュー項目もスロット単位の「Typhoon N」から、表示中の各ソースごとに1行（ラベルは地図上のマーカーと同じ「JTWC12W (NOUL)」「JMA13TS (DOLPHIN)」形式、`_jtwcMarkerLabel`/`_jmaMarkerLabel`として地図マーカー側と共通化）に変更。`PopupMenuButton<int>`が単一intしか値に持てないため、スロット番号×2＋ソース種別（0=JTWC/1=JMA）でエンコード/デコードする方式を採用。`AppStateStorage`のJTWC側フィールドを`jtwcRingsEnabled`にリネーム（JSON上のキー名`"ringsEnabled"`自体は後方互換のため維持、JMA側は従来通り非永続化）。確認済み：3ファイルとも括弧の対応が0で一致。Coworkサンドボックス（Linux、Dart SDKなし）のためビルド未実施——Windows機での確認が必要。

**アプリ起動時のデフォルト設定を変更**（2026-07-27）— ①Range Ring（台風の100/200nm距離同心円）のデフォルトをOffからOnに変更（`_TyphoonSlot.ringsEnabled`初期値）。②InformationメニューのDisplayトグルのデフォルトを、Ship・Typhoon 1のみOn、Typhoon 2/3はOffに変更（`_typhoonSlots`の`List.generate`で`i == 0`のときのみ`displayEnabled = true`とするよう変更。Shipは元々デフォルトOnのため変更なし）。確認済み：括弧・波括弧・角括弧の対応が0で一致。Windows機での実機確認済み（2026-07-27、ユーザー確認OK）。

**船名ラベルの表示化・Passage Plan凡例の新設・距離ラベルの間隔短縮**（2026-07-29）— ユーザー要望3点に対応。①これまで保存のみで表示先が無かったInformationの「Ship's Name」を、船の進行方向後方（distance表示と同じ「behind」方向）に表示するよう変更。複数のPassage Plan（同一船の複数ルート案）全てで同じ値を表示する設計（`ShipMarker.label`の意味をPlan名からShip's Nameへ変更、`map_painter.dart`）。未入力時は空ラベル分のスペースを取らないよう、名前が空なら描画自体をスキップする分岐を追加。②地図上でPlan名を船に添えて表示する方式を廃止し、代わりに画面左上固定（`InteractiveViewer`の外側＝ズームしても画面上のサイズが変わらないプレーンなWidget）の凡例ボックス（タイトル「Passage Plan」＋Display ON中の各プラン名を色分け＋色見本の線、`Column`の`MainAxisSize.min`でプラン数に応じて自動的に縦へ伸びる）を新設（`_buildPassagePlanLegend`、`map_screen.dart`）。凡例の色は地図上のルート色（`_shipColors`）と同じ並び順・インデックスで対応させ、`_buildShipMarkers`と`_buildPassagePlanLegend`の両方に同じ`_activeShipTracks`の結果を渡すことで一致を保証。③台風との距離表示は仕様変更なし（既存の`ShipTyphoonDistance`のまま）。④船の後方に積み上げる「船名→距離1/2/3」の間隔が離れすぎているとの指摘を受け、`_drawShip`内の積み上げ間隔定数を`gap`14→8px・`stackGap`4→2pxに縮小。確認済み：`map_painter.dart`・`map_screen.dart`とも、文字列内の`${}`展開・生文字列・コメントを正しく除外する専用スクリプトで丸括弧・波括弧・角括弧の対応を検証し、両ファイルとも不一致・未対応なし（単純な正規表現ベースの一次チェックでは`${}`展開を誤検知し偽陽性が出たため、より厳密な検証に切り替えた）。Coworkサンドボックス（Linux）のためビルド未実施——Windows機での実機確認が必要（船名ラベルの位置・凡例ボックスの見た目とズーム時のサイズ固定・積み上げ間隔の詰まり具合、いずれも実物で確認いただきたい）。

**Passage Plan凡例の位置調整・台風距離のスロット単位グルーピング**（2026-07-29）— 上記の実機確認後、微調整2点を反映。①Passage Plan凡例ボックスが緯度経度グリッドラベル（画面上端の経度ラベル・画面左端の緯度ラベル、後者はズーム/パン次第で縦のどの位置にも出現しうる）と重なる場合があったため、位置を`left:12,top:12`→`left:48,top:40`に変更（経度ラベル行の下、緯度ラベルの幅より右へクリア）。②船との台風距離表示を、これまでの「表示中の台風マーカーを順番に並べて船から遠ざける」フラットな並びから、台風スロット単位のグルーピングに変更：同一スロットでJTWC/JMA双方を表示中の場合は同じ距離（船からの後方位置）に縦2段で並べ、片方のみなら従来通り1つだけ表示。台風1→2→3の各スロットの組は、これまで通り後ろへ順に配置。実装は`map_screen.dart`に`_typhoonMarkerGroups`（スロットごとのグループ）を新設し`_typhoonMarkers`はそれをフラット化するだけに変更、`_buildShipMarkers`もグループ構造のまま`ShipMarker.typhoonDistances`（型を`List<ShipTyphoonDistance>`→`List<List<ShipTyphoonDistance>>`に変更）へ渡す。`map_painter.dart`の`_drawShip`は各グループにつき、要素が1つなら従来通り、2つなら`_drawDistanceLabel`に新設した`extraOffset`パラメータで上下（screen空間の真上/真下、この描画ブロックは回転させていないため航路の向きに関わらず文字通りの縦方向）にオフセットして描画するよう変更。`shouldRepaint`の差分検出（`_typhoonDistancesChanged`）もネスト構造に合わせて更新。確認済み：文字列内`${}`展開・生文字列・コメントを正しく除外する専用スクリプトで両ファイルとも丸括弧・波括弧・角括弧の対応に不一致・未対応なし。Coworkサンドボックス（Linux）のためビルド未実施——Windows機での実機確認が必要（凡例ボックスがグリッドラベルと重ならないか、JTWC/JMA縦並びの見た目・間隔）。

**Displayをシングル→デュアル切替すると再生位置が先頭に戻るバグを修正（2往復）**（2026-07-29）— `_showLabelSettingsDialog`のSaveハンドラが、スロット0のJTWC/JMAどちらのデータから再生開始時刻を解決したかに関わらず`newStartTime != null`なら無条件に再生位置を先頭へ戻していたため、Display欄を変えただけでもリセットされていた。1回目の修正（解決後の値が変わったかで判定）は「JMA表示→JTWC追加」方向は直ったが「JTWC表示→JMA追加」方向は直らず（JMAを初Fetchすると優先ソースがJTWCからJMAへ切り替わり解決値が必ず変わるため）、2回目の修正で判定基準を「再生の基準になっているソース自体の生データが変わったか」に変更して両方向とも解消。経緯・技術的な判断基準（優先順位のあるフォールバック設計での差分判定の考え方、`identical`による変更検知）は`docs/devlog-playback-anchor-reset.md`参照。確認済み：両修正とも括弧の対応チェックで不一致なし。Windows機で両方向とも実機確認済み（2026-07-29、ユーザー確認OK）。
