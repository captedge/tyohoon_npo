# coastline assets (placeholder)

Real coastline data (clipped to `MapBounds`: N5-50, E115-150) will be placed
here once selected — see TASKS.md "海岸線データの選定・範囲切り出し".
Candidate source: Natural Earth (public domain), 1:50m physical vectors,
clipped and simplified to a lightweight GeoJSON or bundled Dart data file.

Until then, `lib/widgets/map_painter.dart` draws rough placeholder shapes
directly in code (see the `TODO(map-data)` comment there).
