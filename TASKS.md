# TASKS.md — 進行中・次にやること

宣言2（記録の4分割）の②を担当。詳細な経緯は`docs/devlog-<テーマ>.md`、完了記録は`docs/completed-log.md`参照。

## 未着手・進行中

- [x] Windows実機での初回動作確認完了（2026-07-26、`flutter run -d windows`成功。フィードバックは下記「初回実機レビュー」参照）
- [ ] 海岸線データの選定・範囲切り出し（Natural Earth等の無料データから該当範囲のみ抽出、現状は`lib/widgets/map_painter.dart`内の仮ポリゴンで代用中。海岸線・陸地/海の配色は下記レビューで要相談扱い）
- [ ] 船アイコンのデザイン検討（現状は仮の三角形。見た目の話なので後日相談）
- [ ] 気象庁「防災情報XML」の詳細スキーマ調査・パース実装（下記「調査結果」参照）
- [ ] JTWC（米軍）テキストページの読込・パース（日時・緯度経度を抽出）
- [ ] Wi-Fi時まとめ取得＋オフラインキャッシュの仕組み設計
- [ ] 航海計画CSVパーサー実装（下記「実サンプルで確認できた列構成」参照。出発日時の入力UIと、区間距離÷速力での各WP到着時刻の計算ロジックが必要）
- [ ] サンプルデータではなく実データを`MapScreen`に接続（`lib/screens/map_screen.dart`の`TODO(data)`参照）
- [ ] 地図の操作性の既知の制約を解消：ズーム（ボタン/スライダー/マウスホイール）実行時に現在のパン位置がリセットされる（`lib/screens/map_screen.dart`の`_setZoom`内TODO参照。Google Map的な「カーソル位置を中心にズーム」への改善は後回し）

## 気象庁防災情報XML 調査結果（2026-07-26）

- 提供形式：PULL型のみ（PUSH型は2023年9月終了）。ユーザー登録不要、無認証・無料でアクセス可能。
- 台風情報の電文種類：**VPTW60**（台風解析・予報情報、5日先までの予報を含む）
- 公式技術資料：`https://xml.kishou.go.jp/`（トップページ）、`https://xml.kishou.go.jp/tec_material.html`（技術資料一覧）
- 次のステップ：実際のAtomフィードURL（`https://www.data.jma.go.jp/developer/xml/feed/`配下）からVPTW60電文のサンプルを取得し、XMLスキーマ（要素名・座標の単位表記など）を確認してからDartでのパーサー実装に入る。

## JRC ECDIS/NAVTOR CSV 調査結果（2026-07-26）

JRC ECDIS（JAN-701B系）の公式マニュアルで、ルートファイルのCSV書式が確認できた。

- コメント行は`//`で始まる（見出し行もコメット扱い）。区切り文字はカンマ。
- 緯度・経度はそれぞれ「度, 分, 半球（N/S・E/W）」の3列に分かれる（例：`30,6.433,N,129,35.583,E`）。
- 変針点（WP）1行あたりの列構成：`WP No., Lat(度,分,半球), Lon(度,分,半球), Prt[nm]（左舷側の許容偏位）, Stb[nm]（右舷側の許容偏位）, Arr Rad[nm]（到着/変針半径）, Speed[nm]（そのレグの計画速力）, Sail（RL/GC：等角航路か大圏航路か）`
- **日時（ETA/ETD）列は、この純正ECDIS書式には含まれていない**。距離・速力から計算する前提の様子。ユーザーが実際に使うファイルがNAVTOR（passage planning software）経由であれば、その上でETA/ETD列が追加されている可能性がある。
- 結論：ユーザーの指摘通り「規格ものだが、ここで使う以上の情報量がある」ことは公式資料でも裏付けられた。

## 実サンプルで確認できた列構成（2026-07-26、ユーザー提供の実ファイルで確認）

ユーザーが実際に使っているCSV（JRC ECDIS純正のルートファイル）を確認した。公式マニュアルの記載通りで、日時列は無し。

- コメント行：`//`で始まる先頭4行（うち4行目が見出し行）
- データ行の列構成（カンマ区切り、全17列）：
  `WPT No., LATdeg, LATmin, LAThemi(N/S), LONdeg, LONmin, LONhemi(E/W), PORT[NM], STBD[NM], Arr.Rad[NM], Speed[kn], Sail(RL/GC), ROT[deg/min], TurnRad[NM], TimeZone(HH:MM), TimeZoneHemi(E/W), Name`
- 最初のWP（No.000、出発点）は`PORT`〜`TurnRad`までの7列が`***`（該当なし）になる。パーサーは`***`をnull/未使用として扱う。
- 本アプリで実際に使う列は「WPT No., 緯度3列, 経度3列, Speed[kn], Name」のみ。`PORT`/`STBD`/`Arr.Rad`/`Sail`/`ROT`/`TurnRad`/`TimeZone`は距離・時刻計算に不要。
- **日時計算方針（2026-07-26ユーザー確認済み）**：CSVインポート直後に出発日時（WP000の時刻）を入力する画面を挟む。以降は「区間距離（本アプリの中分緯度法で連続する2点から計算）÷その区間のSpeed[kn]」で所要時間を積み上げて、各WPの到着時刻を算出する。これにより`TrackPoint.time`を全WPについて確定でき、既存の線形補間ロジック（`positionAt`）がそのまま使える。
- **将来検討（今は詳細を詰めない、実装時に検討）**：インポート後にWaypointの位置や区間速力をアプリ上で編集（変更）できる機能。

## 初回実機レビュー（2026-07-26、Windows実機で`flutter run`後のフィードバック）

- 操作性：Good（ドラッグ移動・ズームとも良好）
- 視認性：①海岸線はまだラフ案のまま（上記TODO） ②船アイコンは今後検討、航海計画の点線とWaypointがあると良い → **対応済み**：`map_painter.dart`に`_drawShipRoute`を追加し、航海計画全体を点線＋WPマーカーで表示するようにした ③台風はGood
- 再生メニュー：ズームしても画面下部に固定表示のままがよい → 元々別レイアウト（`Column`の外側）にあり固定済みだったことを確認
- その他①：再生時に`dd mmm. yyyy HH:MM (JST)`形式の日時表示があると良く、ズーム時も画面上部などに固定表示がよい → **対応済み**：地図左上に固定表示のラベルを追加（`_formatDateTime`）。現状はサンプルデータの`DateTime.now()`ベースなので「(JST)」表記は仮。実データ接続時に明示的なJST変換に置き換える（`map_screen.dart`の`TODO(timezone)`参照）
- その他②：陸地と海の配色は今後相談 → 海岸線データ確定時にあわせて検討

## 決定待ち・要確認事項

- 気象庁「防災情報XML」は非公式（保証外）のフィードのため、実際のデータ構造を確認してから読み込みロジックを設計する必要あり（上記調査結果参照）
- 出発日時の入力UI（画面のどこに置くか、日時ピッカーの形式）は見た目の話なので後日プロトタイプで確認
- 陸地・海の配色は今後相談（初回実機レビュー参照）
- 船アイコンのデザインは今後検討（初回実機レビュー参照）

## モックアップレビューで確定済み（2026-07-26）

- 地図の見た目（グリッド・アイコン等）の方向性は合意済み。表示範囲をN5-50/E115-150に拡張、緯度経度両方表示、ズームは＋／－ボタン＋スライダー追加、UIは全て英語表記、再生ボタンはトグル式に決定。詳細は`docs/devlog-map-design.md`参照。
