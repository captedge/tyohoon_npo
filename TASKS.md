# TASKS.md — 進行中・次にやること

宣言2（記録の4分割）の②を担当。**このファイルは「未着手・進行中のタスク」と「決定待ち・要確認事項」だけを置く。** 完了したタスクはチェックを付けた時点でここから除去し、`docs/completed-log.md`に1〜2行で記録する（詳細な経緯・技術的な学びがある場合は該当`docs/devlog-<テーマ>.md`へ）。仕様調査結果（CSV/XML形式など）のような「参照資料」は`docs/data-format-notes.md`に集約する。

## 未着手・進行中

- [ ] 海上気象データソースの追加、①沿岸波浪予報（JMA地方海上予報＝VPCY51）から着手（2026-07-29着手）。表示範囲南側（フィリピン近海、北緯5〜19°）は地方海上予報区の対象外のため非表示のままとする方針を確認済み（2026-07-29）。予報区ポリゴンデータ・区域名定数・電文パーサー（`lib/utils/marine_areas.dart`／`marine_area_codes.dart`／`jma_marine_xml_parser.dart`）は実装済み——経緯・詳細は`docs/completed-log.md`と`docs/devlog-online-xml.md`参照。**次にやること**：
  - [ ] `regular.xml`の実データ裏取り・パーサーの実データ確認（最優先、Windows実機確認待ち）：検証用の「Marine forecast (debug)」ボタン（AppBar、波アイコン）を実装済み（2026-07-29、`lib/utils/jma_marine_feed_fetcher.dart`新規／`map_screen.dart`にダイアログ追加）——`regular.xml`を取得し「地方海上予報」を含む電文を全件取得・`parseJmaMarineXml`でパースして結果をそのままテキスト表示する（地図・保存には未反映の検証専用UI）。Agentレビュー実施済み（詳細下記）。**ユーザーに依頼**：Windows機で`flutter run -d windows`後、6/12/18/24時JST前後を狙ってこのボタンを押し、①エラーなく区域・波高が表示されるか、②表示された区域名・波高が気象庁の実際の発表内容と一致するか、を確認して結果を教えてほしい（PARSE ERRORが出た場合はそのエラーメッセージも）。パーサーは公式PDFの記載のみに基づく未検証実装で、Agentレビューで一度実バグ（Area要素の探索方向の誤り）が見つかっている（`docs/devlog-online-xml.md`「Dartパーサー実装・Agentレビューで発見したバグ」参照）——他にも同種の見落としが無いか、実データでの確認時に注意する。
  - [ ] `MapPainter`への予報区ポリゴン塗り分け描画追加、Informationダイアログへの本番用取得UI追加（上記の実データ確認が済んでから着手するのが望ましい。上記のdebugボタンはその前段の検証専用であり本番UIではない）。
  - [ ] ②黒潮＝海しるAPI、③気圧配置図は未着手（`docs/devlog-online-xml.md`参照）。
  - [ ] （Windows実機再確認待ち）Open-Meteo波の場（波高）オーバーレイ：試用ダイアログ・航路周辺グリッド表示とも実装済み（2026-07-29、`lib/utils/build_flags.dart`／`open_meteo_marine_fetcher.dart`／`wave_field.dart`新規、`map_painter.dart`／`map_screen.dart`に追加、`run_windows_personal.bat`／`build_release_personal.bat`新規、本流ビルドには影響なし）。ユーザーが実機で確認し「本当に良い、船長として役立つ」と高評価、**個人用として採用の方向で確定**（2026-07-29）。**バグ修正（2026-07-29、実機再確認待ち）**：「再生しても色が最後まで変わらない」というユーザー報告を受け調査。原因はOpen-Meteoの取得日数（`forecast_days`、既定2日）が航海計画全体の再生時間（数日に及ぶことがある）をカバーしておらず、取得済みデータの末尾を過ぎると値が固定されて見えることだった（Open-Meteoの時系列は`_startTime`ではなく実際の「今日」を起点にする点も一因）。`_fetchWaveFieldGrid`で再生終了時刻から逆算し取得日数を動的に（最大8日まで）指定するよう修正。**ユーザーに依頼**：再ビルド後、同じ操作（再生して波の色の変化）を確認してほしい。**次のステップ（未着手）**：表示範囲を「航路周辺のみ」から「地図の現在の表示範囲（画面に映っている分だけ）」方式に変更——ユーザーからの「エリア全体に展開したい」という要望に対し、固定の全表示範囲（N5-50/E85-170）を一括取得する方式は①データ量（格子点2万件超）②描画の重さ③粗い格子での見た目劣化、の3点でデメリットが大きいことを説明し、パン・ズームで見えている範囲だけを都度取得する方式（③案）を採用することで合意（2026-07-29、経緯・検討した3案は`docs/devlog-online-xml.md`「波の場オーバーレイの表示範囲方式」参照）。実装未着手（次回セッション着手）。波の色分け（0-4mを4段階、気象庁公式階級とは別の独自配色）についてもユーザーから質問があり回答済み——変更希望があれば別途対応。実際の海況が穏やかで波高差が小さい場合、現在の色分け幅（0-4m）では変化が視覚的に分かりにくい可能性がある点も留意（色スケールの調整は別途要望があれば対応）。
  - [ ] `regular.xml`実データ確認の結果次第で、①メイン（配布可能）側にVPCY51を採用するか判断（2026-07-29、ユーザー方針：「不採用の場合はこれ以上の実装はしない」）。上記②個人用Open-Meteoとは独立の判断——採否にかかわらず②の作業には影響しない。
- [ ] CSVライブラリの上限50件到達時のエラー表示のみ未確認（他の全項目はWindows実機で確認済み、`docs/completed-log.md`参照）。50件貯める機会があれば確認する
- [ ] （Windows実機確認待ち）複数台風同時発表対応・Import Allボタン（2026-07-29実装、1件のみの場合の動作は実機確認済み）：「Import All (JMA)」「Import All (JTWC)」で2件以上同時に見つかった場合、Typhoon 1/2/3それぞれ独立してDisplay On/Off切替できるか（コード上は独立していることを確認済みだが実機未確認）。あわせて完了メッセージ「Imported to Typhoon 1, 2」等の表記も確認。「Fetch」→「Import」への表記統一箇所（Import from JMA/JTWC、Import All）も見た目を確認。

## 決定待ち・要確認事項

- スマホ対応（Android/iOS）は機能・デザインをデスクトップ版と極力共通にし、横向き固定・画面サイズ差分とタッチ操作のみ個別調整する方針（仮、`docs/devlog-architecture-roadmap.md`参照）。着手時に改めて確認
- より高精細な海岸線データ（1:10m相当）への差し替え（優先度低、`docs/devlog-map-design.md`参照）
- 航海計画編集画面の緯度経度入力は現在10進度（decimal degrees）。他画面の度分（deg-min）表記と揃えるかは今後ユーザーに確認
- Passage Plan「登録済みプラン」自体の名前は現状CSVファイル名固定（拡張子除く）で、後から改名するUIは未実装（Renameが可能なのはCSVライブラリ側のファイル名のみで、既に登録済みのプランの表示名には反映されない仕様でユーザー確認済み・現状維持でOK、2026-07-27）。将来的に改名したい場合は別途要望を
- 船アイコンを色ごとに10種類（専用PNG画像）用意したい、とのユーザー希望あり。現在は`assets/ship_icon01.png`を`ColorFilter`で機械的に着色しているだけなので、専用画像を用意する場合はカラーコード一覧（`docs/devlog-passage-plan-multi.md`参照）に合わせて作成し、画像が揃い次第、着色方式から画像切替方式へ変更するか相談する
