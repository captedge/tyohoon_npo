# TASKS.md — 進行中・次にやること

宣言2（記録の4分割）の②を担当。詳細な経緯は`docs/devlog-<テーマ>.md`、完了記録は`docs/completed-log.md`参照。

## 未着手・進行中

- [x] Windows実機での初回動作確認完了（2026-07-26、`flutter run -d windows`成功。フィードバックは下記「初回実機レビュー」参照）
- [x] 海岸線データの選定・範囲切り出し（2026-07-26対応。下記「海岸線データ差し替え」参照）
- [x] 地図の操作性の改善：ズーム（ボタン/スライダー/マウスホイール）を、操作した位置（マウスホイールはカーソル位置、ボタン/スライダーは画面中心）を軸にズームするよう変更し、パン位置がリセットされる問題を解消（2026-07-26、`map_screen.dart`の`_zoomAtViewportPoint`）
- [x] 地図の投影法をWeb Mercatorに変更し、表示比率を固定（2026-07-26。下記「投影法・表示比率の見直し」参照）
- [x] より高精細な海岸線データ（1:50m）への差し替え（2026-07-27対応。下記「海岸線データ1:50m化」参照。1:10mへのさらなる高精細化は優先度低・現状据え置き）
- [x] 陸地/海の配色調整：ナビチャート風の配色案（淡い青の海／ベージュの陸）をユーザーが実機で確認し、2026-07-27に確定（下記「配色の暫定変更」参照）
- [x] 緯度経度グリッドラベル（「20N」等）が地図の端に埋め込みでズーム/パンすると画面外に出てしまう問題を修正。画面端に追従表示するオーバーレイに変更（2026-07-27対応。下記「グリッドラベルの追従表示化」参照）
- [x] カーソル位置の緯度経度を画面右下に追従表示（「31-15.5N 140-23.4E」形式）。2026-07-27対応、下記「カーソル位置緯度経度の表示」参照
- [x] 船アイコン：頂点が常に次のWPを向くよう変更（三角形の形自体は現状維持）。2026-07-27対応、下記「船アイコンの向き変更」参照
- [x] `flutter run -d windows`のバッチファイル化（ダブルクリック起動）。2026-07-27対応、下記「起動用batファイル」参照
- [ ] 気象庁「防災情報XML」の詳細スキーマ調査・パース実装（下記「調査結果」参照）
- [ ] JTWC（米軍）テキストページの読込・パース（日時・緯度経度を抽出）
- [ ] Wi-Fi時まとめ取得＋オフラインキャッシュの仕組み設計
- [ ] 航海計画CSVパーサー実装（下記「実サンプルで確認できた列構成」参照。出発日時の入力UIと、区間距離÷速力での各WP到着時刻の計算ロジックが必要）
- [ ] サンプルデータではなく実データを`MapScreen`に接続（`lib/screens/map_screen.dart`の`TODO(data)`参照）

## 海岸線データ差し替え（2026-07-26）

これまで`map_painter.dart`にハードコードしていた仮の海岸線を、実データに差し替えた。

- データ源：Natural Earth 1:110m land data（パブリックドメイン）。`world-atlas`パッケージのTopoJSON版（`land-110m.json`）を取得（生のGeoJSON版はファイルサイズが大きく、このセッションで使える取得手段では全体を取得できなかったため、より軽量なTopoJSON版を採用）。
- 処理：TopoJSONのアークをデコードしてポリゴン化 → 表示範囲（N5-50/E115-150）でクリップ → 簡略化（許容誤差0.02度）→ 座標を小数点3桁に丸め。結果14ポリゴン・約300点・約5KB。
- 保存先：`typhoon_ship_tracker/assets/coastline/coastline.json`（生成手順・既知の制約は同フォルダの`README.md`参照）
- 実装：`lib/utils/coastline.dart`（アセット読込・パース）、`lib/widgets/map_painter.dart`の`_drawCoastline`（描画）、`lib/screens/map_screen.dart`（`initState`で非同期読込→`setState`で反映）
- **既知の制約**：1:110m解像度は世界地図向けの粗さで、海岸線の詳細さは限定的（大まかな形は認識できるレベル）。より高精細な1:50m/1:10mデータは、今回使えた取得手段では一括取得できなかった（ファイルサイズ超過）。
- **高精細化への道筋（2026-07-26確認）**：Cowork内のファイル取得ツールには1回あたりのサイズ上限があり、Natural Earth 1:50m/1:10mの世界全体版は取得できなかった（1:110mはたまたまサイズ内に収まった）。国別に分割された代替データ源（`world.geo.json`等）も試したが、既存の1:110mクリップ版と同程度かそれ以下の精度だった。**回避策**：ユーザーが`naturalearthdata.com`から1:50m/1:10mデータをブラウザで直接ダウンロードし、プロジェクトフォルダに置けば、Coworkはローカルファイルとして読み込めるため取得サイズ制限を回避できる。優先度低のためユーザー判断待ち。
- 陸地・海の配色は仮のまま（ユーザーと相談待ち、上記タスク参照）。

## 海岸線データ1:50m化（2026-07-27）

前回セッションでは「Natural Earthの1:50m/1:10mデータは取得手段のサイズ制限で入手できず、ユーザーによる手動ダウンロードが必要」と判断していたが、今回のセッションでは`registry.npmjs.org`（npmレジストリ）が直接取得可能だったため、`world-atlas`パッケージの旧バージョン（v1.1.4、`world/50m.json`）から1:50mデータを取得できた。**教訓（宣言3の③）**：`naturalearthdata.com`や`raw.githubusercontent.com`は取得できなくても、同じデータを配布している別ホスト（npm/PyPIレジストリ等）が使える場合があるので、「取得不可＝ユーザー手動対応待ち」と判断する前に、代替ホストを一通り試す。

- データ源：`world-atlas@1.1.4`の`world/50m.json`（TopoJSON、`land`オブジェクト）。
- 処理：Python + shapelyでTopoJSONアークをデコード→ポリゴン化 → 表示範囲付近のポリゴンのみ抽出（境界ボックスで粗く絞り込み）→ 不正な形状は`buffer(0)`で修復 → `unary_union`で結合 → 表示範囲（N5-50/E115-150）でクリップ → 簡略化（許容誤差0.01度、1:110m版の0.02度より高精度）→ 座標を小数点3桁に丸め。結果123ポリゴン・約3,000点・約50KB（従来の1:110m版は14ポリゴン・約300点・約5KB）。
- 差し替え前にmatplotlibで描画して目視確認済み：日本（九州・四国含む）・朝鮮半島・台湾・中国沿岸・フィリピン諸島の形状が明確に認識できるレベルに向上。
- 保存先・実装は従来通り（`typhoon_ship_tracker/assets/coastline/coastline.json`、`lib/utils/coastline.dart`、`lib/widgets/map_painter.dart`）。コード側のフォーマット（`[[lon,lat],...]`の配列）は変更なしのため、データ差し替えのみで反映される。
- **既知の制約**：1:50mは中程度の精度（島や湾の形状は認識できる）。1:10m相当のさらなる高精細化は今回試みておらず（`world-atlas`パッケージには1:10mデータが含まれないため別ソースが必要）、優先度低のため現状据え置き。

## 配色の暫定変更（2026-07-27、ユーザー確認済み）

海岸線の高精細化にあわせて、従来の仮配色（白い海／灰色の陸）から、ナビチャート風の配色案に変更。実機確認の上、ユーザーから「とても良い」と確定の返答あり。

- 海：淡い青（`#D8EAF6`）、陸：ベージュ（`#E7DFC6`）、陸の輪郭：くすんだ黄土色（`#9C8F66`）、グリッド線：青灰色（`Colors.blueGrey`、不透明度0.35）。
- 船の航海計画ラインは、淡い青の海との視認性を考慮して`Colors.blue.shade300`→`Colors.blue.shade600`に濃くした。台風・船マーカーの配色は変更なし（既存の色でも視認性は問題ないと判断）。
- 実装：`lib/widgets/map_painter.dart`の`_seaColor`/`_landColor`/`_landOutlineColor`（定数として一箇所にまとめてあり、色の変更依頼があればここだけ直せばよい）。

## グリッドラベルの追従表示化（2026-07-27）

実機確認後のフィードバック：「20N」等の緯度経度グリッドラベルが地図の端（実際の地図座標上の端）に埋め込み描画されていたため、ズームイン・パンすると画面外に出て見えなくなる問題があった。ユーザーからの希望は「ズームしても必ず見えるよう画面に追従表示、画面端が陸上でもグリッド端に追従表示」。

- **原因**：ラベルのテキストが`map_painter.dart`の`_drawGrid`内で、地図の論理キャンバス（`MapBounds.canvasSize`、`InteractiveViewer`がズーム/パンする対象）にグリッド線と一緒に焼き込み描画されていたため、地図と同じようにスクロールしてしまっていた。
- **対応**：`_drawGrid`からテキスト描画を削除し、グリッド線のみ描画するように変更（線自体は地図と一緒にスクロールするのが正しい挙動なのでそのまま）。ラベルは`map_screen.dart`の新規メソッド`_buildGridLabelOverlay`で、`InteractiveViewer`とは別のオーバーレイ（`Positioned.fill`＋`AnimatedBuilder`）として実装。`TransformationController`の現在の変換（平行移動・拡大率）から各経線・緯線の画面上のX/Y座標を計算し、その線が現在の画面内に入っていれば、緯度ラベルは画面左端（X=4）、経度ラベルは画面上端（Y=4）に固定表示する（画面内のどこに地図がスクロールしていても、ラベルは常に画面の同じ辺に張り付く）。
- **リアルタイム追従の工夫**：既存の`_zoom`/`_translation`状態はドラッグ操作の**完了後**（`onInteractionEnd`）にしか更新されないため、そのままだとドラッグ中はラベルが古い位置のまま止まって見える。`AnimatedBuilder`で`_transformationController`自体を直接listenする方式にすることで、ドラッグ・ピンチ中もリアルタイムに追従するようにした。
- ラベルはチップ状の白背景（半透明）付きで表示し、地図が海でも陸でも視認できるようにした。既存の日時表示（画面左上）とグリッドラベルが重なる可能性がある稀なケースでは、日時表示を後から描画する順序にしてあり、日時表示が上に乗るようにしてある。

## カーソル位置緯度経度の表示（2026-07-27）

要望：マウスカーソル位置の緯度経度を画面右下に追従表示。表示形式は「31-15.5N 140-23.4E」（度-分.小数第1位、秒は60進法で分の小数に換算：15分30秒→15.5分）。

- `lib/utils/map_bounds.dart`に`toOffset`の逆変換`MapBounds.fromOffset`（Web Mercator逆変換、キャンバス座標→緯度経度）を追加。往復変換が一致することをPythonで事前検証済み。
- `map_screen.dart`に`MouseRegion`（`onHover`/`onExit`）を追加し、カーソルのビューポート座標を`TransformationController`の現在の変換で地図座標に変換→`MapBounds.fromOffset`で緯度経度化→`_cursorLatLon`状態に保持。
- 表示形式は`_formatDegMin`で実装：度を切り捨て、残りを分に変換して小数第1位に丸め、丸めで60.0分になった場合は繰り上げ（度+1、分0.0）で対応。
- 表示位置は画面右下だが、既存のズームボタン列（画面右端、上下いっぱい）と重ならないよう、その左側（`right: 64`）に配置。
- タッチ操作のみのデバイス（将来のスマホ展開時）では`onHover`は発火しないため、この表示は出ない（カーソルが存在しないため仕様として妥当。スマホ版でのタップ位置表示が必要になれば別途検討）。

## 船アイコンの向き変更（2026-07-27）

要望：船アイコンの二等辺三角形の形はそのままで、頂点が常に次のWP（変針点）を向くようにする。

- `map_painter.dart`の`MapPainter`に`nextWaypoint`（`LatLng?`）フィールドを追加。`map_screen.dart`側で、現在時刻より後の最初の航海計画ポイントを`_nextWaypointAfter`で探して渡す（航海完了後で次のWPが無い場合は`null`→従来通り北向き固定にフォールバック）。
- `_drawShip`で、現在の船位置から`nextWaypoint`への方位角を、地図の投影後キャンバス座標（`MapBounds.toOffset`の結果）上で`atan2`で計算し、`canvas.translate`＋`canvas.rotate`で三角形を回転させてから描画。Web Mercatorは等角図法（局所的に角度を保つ）なので、この規模の地図であればキャンバス座標上の角度計算で実用上問題ない。

## 起動用batファイル（2026-07-27）

要望：`flutter run -d windows`をダブルクリックで起動できるようにしたい。

- プロジェクトルート（`commit.bat`と同じ階層）に`run_windows.bat`を作成。中身は`flutter`をPATHに追加（ユーザー環境変数への追加が新しいセッションに反映されないことがあるため念のため明示）→`typhoon_ship_tracker/`へ移動→`flutter run -d windows`実行→終了後`pause`でウィンドウを閉じずエラー内容を確認できるようにした。
- `docs/flutter-windows-env-notes.md`の`.bat`納品前チェックリストに従い確認済み：CRLF変換済み（`xxd`で`0d0a`を確認）、`if`/`for`括弧ブロックなし、日本語直書きなし（`echo`のメッセージも含め全て英数字）。
- 実行にはVisual Studioの「Desktop development with C++」ワークロードが別途必要（`docs/flutter-windows-env-notes.md`の既知の癖6参照、今回のbatはその前提を変えるものではない）。
- あわせて`commit.bat`の`git add`対象に`run_windows.bat`を追加。また、`typhoon_ship_tracker`フォルダを丸ごと`git add`する方式から、今回変更した個別ファイルを列挙する方式に変更した（`windows/`配下等のCRLF差分ノイズ（既知の癖7、実害なしと確認済み）を今回のコミットに巻き込まないため）。次回以降、フォルダ丸ごと追加に戻したい場合はユーザー判断で`commit.bat`を書き換えてよい。

## 投影法・表示比率の見直し（2026-07-26）

初回実機レビューで「メルカトル図法に対して緯度経度のグリッド比率が悪く、縦に縮んで見える」との指摘を受けて対応。

- **原因**：それまでの実装は単純な等角経緯度図法（緯度・経度をそれぞれ独立にウィジェットの幅・高さへ線形マッピング）で、かつ地図の描画サイズがウィンドウの形に応じて伸縮していた。経度方向の実距離は緯度が高くなるほどcos(緯度)倍に縮むが、この補正がなかったため、日本のような中緯度地域の形が実際より東西に間延びして（＝縦に縮んで）見えていた。
- **対応**：`lib/utils/map_bounds.dart`をWeb Mercator（Google Mapsと同じ投影法）に変更。あわせて、表示範囲（N5-50/E115-150）のMercator換算での正しい縦横比を持つ固定サイズの論理キャンバス（`MapBounds.canvasSize`、幅900×高さ約1360）を導入し、`InteractiveViewer`はこのキャンバスをズーム・パンする方式にした（ウィンドウの形に応じて地図が伸縮しなくなった）。
- **初期表示位置**：起動時に表示範囲全体を縮小表示するのではなく、日本付近（北緯30°・東経135°）を中心に少しズームインした状態で開始するよう変更（`MapBounds.defaultCenterLat/Lon`）。全体を見たい場合はズームアウトすればよく、その際は上下（北海道より北・フィリピン南部より南）が画面外にはみ出す形になる（本人希望通り）。
- 関連コード：`lib/utils/map_bounds.dart`（投影・キャンバスサイズ計算）、`lib/widgets/map_painter.dart`（`toOffset`から`size`引数を削除し固定キャンバス基準に統一）、`lib/screens/map_screen.dart`（`_updateViewportSize`で初期表示位置とズーム範囲を算出）。

## 気象庁防災情報XML 調査結果（2026-07-26）

- 提供形式：PULL型のみ（PUSH型は2023年9月終了）。ユーザー登録不要、無認証・無料でアクセス可能。
- 台風情報の電文種類：**VPTW60**（台風解析・予報情報、5日先までの予報を含む）
- 公式技術資料：`https://xml.kishou.go.jp/`（トップページ）、`https://xml.kishou.go.jp/tec_material.html`（技術資料一覧）
- 次のステップ：実際のAtomフィードURL（`https://www.data.jma.go.jp/developer/xml/feed/`配下）からVPTW60電文のサンプルを取得し、XMLスキーマ（要素名・座標の単位表記など）を確認してからDartでのパーサー実装に入る。

## JRC ECDIS/NAVTOR CSV 調査結果（2026-07-26）

JRC ECDIS（JAN-701B系）の公式マニュアルで、ルートファイルのCSV書式が確認できた。

- コメント行は`//`で始まる（見出し行もコメット扱い）。区切り文字はカンマ。
- 緯度・経度はそれぞれ「度, 分, 半球（N/S・E/W）」の3列に分かれる（例：`30,6.433,N,129,35.583,E`）。
- 変針点（WP）1行あたりの列構成：`WP No., Lat(度,分,半球), Lon(度,分,半球), Prt[nm]（左舷側の許容偏位）, Stb[nm]（右舷側の許容偏位）, Arr Rad[nm]（到着/変針半径）, Speed[nm]（そのレグの計画速力）, Sail（RL/GC：等角航路か大圏航路か）`
- **日時（ETA/ETD）列は、この純正ECDIS書式には含まれていない**。距離・速力から計算する前提の様子。ユーザーが実際に使うファイルがNAVTOR（passage planning software）経由であれば、その上でETA/ETD列が追加されている可能性がある。
- 結論：ユーザーの指摘通り「規格ものだが、ここで使う以上の情報量がある」ことは公式資料でも裏付けられた。

## 実サンプルで確認できた列構成（2026-07-26、ユーザー提供の実ファイルで確認）

ユーザーが実際に使っているCSV（JRC ECDIS純正のルートファイル）を確認した。公式マニュアルの記載通りで、日時列は無し。

- コメント行：`//`で始まる先頭4行（うち4行目が見出し行）
- データ行の列構成（カンマ区切り、全17列）：
  `WPT No., LATdeg, LATmin, LAThemi(N/S), LONdeg, LONmin, LONhemi(E/W), PORT[NM], STBD[NM], Arr.Rad[NM], Speed[kn], Sail(RL/GC), ROT[deg/min], TurnRad[NM], TimeZone(HH:MM), TimeZoneHemi(E/W), Name`
- 最初のWP（No.000、出発点）は`PORT`〜`TurnRad`までの7列が`***`（該当なし）になる。パーサーは`***`をnull/未使用として扱う。
- 本アプリで実際に使う列は「WPT No., 緯度3列, 経度3列, Speed[kn], Name」のみ。`PORT`/`STBD`/`Arr.Rad`/`Sail`/`ROT`/`TurnRad`/`TimeZone`は距離・時刻計算に不要。
- **日時計算方針（2026-07-26ユーザー確認済み）**：CSVインポート直後に出発日時（WP000の時刻）を入力する画面を挟む。以降は「区間距離（本アプリの中分緯度法で連続する2点から計算）÷その区間のSpeed[kn]」で所要時間を積み上げて、各WPの到着時刻を算出する。これにより`TrackPoint.time`を全WPについて確定でき、既存の線形補間ロジック（`positionAt`）がそのまま使える。
- **将来検討（今は詳細を詰めない、実装時に検討）**：インポート後にWaypointの位置や区間速力をアプリ上で編集（変更）できる機能。

## 初回実機レビュー（2026-07-26、Windows実機で`flutter run`後のフィードバック）

- 操作性：Good（ドラッグ移動・ズームとも良好）
- 視認性：①海岸線はまだラフ案のまま（上記TODO） ②船アイコンは今後検討、航海計画の点線とWaypointがあると良い → **対応済み**：`map_painter.dart`に`_drawShipRoute`を追加し、航海計画全体を点線＋WPマーカーで表示するようにした ③台風はGood
- 再生メニュー：ズームしても画面下部に固定表示のままがよい → 元々別レイアウト（`Column`の外側）にあり固定済みだったことを確認
- その他①：再生時に`dd mmm. yyyy HH:MM (JST)`形式の日時表示があると良く、ズーム時も画面上部などに固定表示がよい → **対応済み**：地図左上に固定表示のラベルを追加（`_formatDateTime`）。現状はサンプルデータの`DateTime.now()`ベースなので「(JST)」表記は仮。実データ接続時に明示的なJST変換に置き換える（`map_screen.dart`の`TODO(timezone)`参照）
- その他②：陸地と海の配色は今後相談 → 海岸線データ確定時にあわせて検討

## 決定待ち・要確認事項

- 気象庁「防災情報XML」は非公式（保証外）のフィードのため、実際のデータ構造を確認してから読み込みロジックを設計する必要あり（上記調査結果参照）
- 出発日時の入力UI（画面のどこに置くか、日時ピッカーの形式）は見た目の話なので後日プロトタイプで確認
- より高精細な海岸線データへの差し替え（優先度低、上記「海岸線データ差し替え」の既知の制約参照）

## モックアップレビューで確定済み（2026-07-26）

- 地図の見た目（グリッド・アイコン等）の方向性は合意済み。表示範囲をN5-50/E115-150に拡張、緯度経度両方表示、ズームは＋／－ボタン＋スライダー追加、UIは全て英語表記、再生ボタンはトグル式に決定。詳細は`docs/devlog-map-design.md`参照。
